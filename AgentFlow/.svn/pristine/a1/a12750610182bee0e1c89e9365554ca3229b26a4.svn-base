<!-- Generated by Trae Work -->
<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>前端与后端功能详细设计</title>
<style>
  :root {
    --bg: #f8f9fb;
    --bg2: #ffffff;
    --ink: #1a2332;
    --muted: #6b7a90;
    --rule: #e2e8f0;
    --accent: #2563eb;
    --accent2: #0891b2;
    --success: #059669;
    --warning: #d97706;
    --danger: #dc2626;
    --fe-bg: #eff6ff;
    --be-bg: #ecfdf5;
  }

  * { margin: 0; padding: 0; box-sizing: border-box; }

  body {
    font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", "Noto Sans CJK SC", "Microsoft YaHei", sans-serif;
    background: var(--bg);
    color: var(--ink);
    line-height: 1.7;
    font-size: 15px;
  }

  .container {
    max-width: 1120px;
    margin: 0 auto;
    padding: 2rem 1.5rem;
  }

  /* Header */
  .report-header {
    background: linear-gradient(135deg, #1e3a5f 0%, #2563eb 50%, #0891b2 100%);
    color: #fff;
    padding: 2.5rem 2rem;
    border-radius: 12px;
    margin-bottom: 2rem;
    position: relative;
    overflow: hidden;
  }
  .report-header::before {
    content: '';
    position: absolute;
    top: -50%;
    right: -10%;
    width: 400px;
    height: 400px;
    background: radial-gradient(circle, rgba(255,255,255,0.1) 0%, transparent 70%);
    border-radius: 50%;
  }
  .report-header h1 {
    font-size: 1.8rem;
    font-weight: 700;
    margin-bottom: 0.5rem;
    position: relative;
  }
  .report-header .subtitle {
    font-size: 0.95rem;
    opacity: 0.85;
    position: relative;
  }

  /* Section */
  section { margin-bottom: 2.5rem; }
  h2 {
    font-size: 1.35rem;
    font-weight: 700;
    margin-bottom: 1rem;
    padding-bottom: 0.5rem;
    border-bottom: 2px solid var(--accent);
    display: flex;
    align-items: center;
    gap: 0.6rem;
  }
  h2 .num {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 28px;
    height: 28px;
    background: var(--accent);
    color: #fff;
    border-radius: 6px;
    font-size: 0.85rem;
    font-weight: 700;
  }
  h2.fe { border-bottom-color: var(--accent); }
  h2.fe .num { background: var(--accent); }
  h2.be { border-bottom-color: var(--success); }
  h2.be .num { background: var(--success); }

  h3 {
    font-size: 1.1rem;
    font-weight: 600;
    margin: 1.3rem 0 0.6rem;
    padding-left: 0.7rem;
    border-left: 3px solid var(--accent);
  }
  h3.fe { border-left-color: var(--accent); }
  h3.be { border-left-color: var(--success); }

  h4 {
    font-size: 0.98rem;
    font-weight: 600;
    margin: 1rem 0 0.4rem;
    color: var(--accent);
  }
  p { margin-bottom: 0.7rem; }

  /* Tabs */
  .tabs {
    display: flex;
    gap: 0;
    margin-bottom: 1.5rem;
    border-bottom: 2px solid var(--rule);
  }
  .tab-btn {
    padding: 0.7rem 1.5rem;
    border: none;
    background: none;
    cursor: pointer;
    font-size: 0.95rem;
    font-weight: 600;
    color: var(--muted);
    border-bottom: 2px solid transparent;
    margin-bottom: -2px;
    transition: all 0.2s;
    font-family: inherit;
  }
  .tab-btn.active {
    color: var(--accent);
    border-bottom-color: var(--accent);
  }
  .tab-btn.be-tab.active {
    color: var(--success);
    border-bottom-color: var(--success);
  }
  .tab-content { display: none; }
  .tab-content.active { display: block; }

  /* Cards */
  .card {
    background: var(--bg2);
    border: 1px solid var(--rule);
    border-radius: 8px;
    padding: 1.1rem;
    margin-bottom: 1rem;
  }
  .card.fe { border-top: 3px solid var(--accent); }
  .card.be { border-top: 3px solid var(--success); }
  .card h4 { margin-top: 0; }
  .card-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 1rem;
    margin: 1rem 0;
  }
  .card-grid .card { margin-bottom: 0; }

  /* Tables */
  .table-wrap {
    overflow-x: auto;
    overflow-y: auto;
    max-height: 600px;
    margin: 1rem 0;
    border: 1px solid var(--rule);
    border-radius: 6px;
  }
  table {
    width: 100%;
    border-collapse: collapse;
    font-size: 0.88rem;
  }
  thead th {
    background: var(--accent);
    color: #fff;
    padding: 0.65rem 0.8rem;
    text-align: left;
    font-weight: 600;
    position: sticky;
    top: 0;
    z-index: 1;
    white-space: nowrap;
  }
  thead.fe th { background: var(--accent); }
  thead.be th { background: var(--success); }
  tbody td {
    padding: 0.55rem 0.8rem;
    border-bottom: 1px solid var(--rule);
    vertical-align: top;
  }
  tbody tr:hover { background: #f1f5f9; }
  tbody tr:last-child td { border-bottom: none; }

  /* Code */
  code {
    font-family: "Consolas", "Monaco", monospace;
    background: #f1f5f9;
    padding: 0.15rem 0.4rem;
    border-radius: 4px;
    font-size: 0.82em;
    color: var(--accent2);
  }
  pre {
    background: #1e293b;
    color: #e2e8f0;
    padding: 0.9rem 1.1rem;
    border-radius: 6px;
    overflow-x: auto;
    margin: 0.7rem 0;
    font-size: 0.82rem;
    line-height: 1.6;
  }
  pre code {
    background: none;
    padding: 0;
    color: inherit;
  }

  /* Diagram */
  .diagram {
    margin: 1.2rem 0;
    text-align: center;
  }
  .diagram figcaption {
    font-size: 0.8rem;
    color: var(--muted);
    margin-top: 0.5rem;
    font-style: italic;
  }
  .mermaid {
    background: var(--bg2);
    border: 1px solid var(--rule);
    border-radius: 8px;
    padding: 1rem;
  }

  /* Callout */
  .callout {
    border-left: 4px solid var(--accent);
    background: #eff6ff;
    padding: 0.8rem 1.1rem;
    border-radius: 0 6px 6px 0;
    margin: 0.8rem 0;
    font-size: 0.9rem;
  }
  .callout.success {
    border-color: var(--success);
    background: #ecfdf5;
  }
  .callout.warning {
    border-color: var(--warning);
    background: #fffbeb;
  }

  /* Feature list */
  .feature-list {
    list-style: none;
    padding-left: 0;
    margin: 0.5rem 0;
  }
  .feature-list li {
    padding: 0.3rem 0 0.3rem 1.5rem;
    position: relative;
    font-size: 0.9rem;
  }
  .feature-list li::before {
    content: '✓';
    position: absolute;
    left: 0;
    color: var(--success);
    font-weight: 700;
  }
  .fe-list li::before { color: var(--accent); }

  /* Class box */
  .class-box {
    background: #f8fafc;
    border: 1px solid var(--rule);
    border-radius: 6px;
    margin: 0.6rem 0;
    font-size: 0.85rem;
    overflow: hidden;
  }
  .class-box .class-name {
    background: var(--accent);
    color: #fff;
    padding: 0.4rem 0.8rem;
    font-weight: 600;
    font-family: "Consolas", monospace;
    font-size: 0.85rem;
  }
  .class-box.be .class-name { background: var(--success); }
  .class-box .class-section {
    padding: 0.4rem 0.8rem;
    border-bottom: 1px solid var(--rule);
  }
  .class-box .class-section:last-child { border-bottom: none; }
  .class-box .section-label {
    font-weight: 600;
    font-size: 0.75rem;
    color: var(--muted);
    text-transform: uppercase;
    letter-spacing: 0.05em;
    margin-bottom: 0.2rem;
  }
  .class-box ul {
    list-style: none;
    padding-left: 0;
    margin: 0;
  }
  .class-box li {
    padding: 0.15rem 0;
    font-family: "Consolas", monospace;
    font-size: 0.8rem;
  }
  .class-box li .pub { color: var(--success); }
  .class-box li .pri { color: var(--warning); }

  /* Two col */
  .two-col {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1rem;
  }
  @media (max-width: 768px) {
    .two-col { grid-template-columns: 1fr; }
    .card-grid { grid-template-columns: 1fr; }
  }

  mark.key {
    background: none;
    color: var(--accent);
    font-weight: 600;
  }

  ul, ol {
    margin-left: 1.2rem;
    margin-bottom: 0.7rem;
  }
  li { margin-bottom: 0.25rem; }

  footer {
    margin-top: 3rem;
    padding-top: 1.5rem;
    border-top: 1px solid var(--rule);
    color: var(--muted);
    font-size: 0.85rem;
    text-align: center;
  }

  /* Wireframe mock */
  .wireframe {
    background: var(--bg2);
    border: 1px solid var(--rule);
    border-radius: 8px;
    padding: 1rem;
    margin: 1rem 0;
  }
  .wireframe .wf-title {
    font-weight: 600;
    font-size: 0.9rem;
    margin-bottom: 0.6rem;
    color: var(--accent);
  }
  .wf-toolbar {
    display: flex;
    gap: 0.5rem;
    padding: 0.5rem;
    background: #f1f5f9;
    border-radius: 4px;
    margin-bottom: 0.6rem;
  }
  .wf-btn {
    padding: 0.25rem 0.6rem;
    background: var(--accent);
    color: #fff;
    border-radius: 3px;
    font-size: 0.75rem;
  }
  .wf-btn.sec { background: var(--muted); }
  .wf-body {
    display: flex;
    gap: 0.6rem;
    min-height: 180px;
  }
  .wf-sidebar {
    width: 140px;
    background: #f8fafc;
    border: 1px dashed var(--rule);
    border-radius: 4px;
    padding: 0.5rem;
    font-size: 0.75rem;
    color: var(--muted);
  }
  .wf-main {
    flex: 1;
    background: #f8fafc;
    border: 1px dashed var(--rule);
    border-radius: 4px;
    padding: 0.5rem;
    font-size: 0.75rem;
    color: var(--muted);
  }
  .wf-props {
    width: 160px;
    background: #f8fafc;
    border: 1px dashed var(--rule);
    border-radius: 4px;
    padding: 0.5rem;
    font-size: 0.75rem;
    color: var(--muted);
  }
</style>
</head>
<body>
<div class="container">

  <header class="report-header">
    <h1>前端与后端功能详细设计</h1>
    <div class="subtitle">Qt 自动化测试配置软件 · 前后端职责边界与功能清单</div>
  </header>

  <!-- 总览 -->
  <section>
    <h2><span class="num">0</span>前后端职责边界总览</h2>

    <figure class="diagram">
      <pre class="mermaid">
graph TD
    subgraph FE["🎨 前端（Qt Widgets）"]
        direction LR
        FE1[界面渲染]
        FE2[用户交互]
        FE3[表单校验]
        FE4[可视化展示]
        FE5[本地状态管理]
        FE6[信号槽连接]
    end

    subgraph BLL_BE["⚙️ 后端（业务逻辑 + 数据访问）"]
        direction LR
        BE1[业务规则]
        BE2[数据持久化]
        BE3[流程引擎]
        BE4[事务管理]
        BE5[AI 调度]
        BE6[版本控制]
    end

    FE -->|Service 接口调用| BLL_BE
    BLL_BE -->|信号/返回值| FE
      </pre>
      <figcaption>图 0-1：前后端职责边界</figcaption>
    </figure>

    <div class="two-col">
      <div class="card fe">
        <h4>🎨 前端负责什么</h4>
        <ul class="feature-list fe-list">
          <li>所有界面元素的绘制与布局</li>
          <li>用户操作的捕获与响应（点击、拖拽、输入）</li>
          <li>输入数据的前端格式校验（非空、格式、范围）</li>
          <li>数据的可视化展示（表格、图形、流程画布）</li>
          <li>界面状态管理（选中项、展开/折叠、Tab切换）</li>
          <li>后端数据到UI模型的转换与绑定</li>
          <li>操作反馈提示（消息框、进度条、状态提示）</li>
          <li>界面级别的撤销/重做（编辑操作）</li>
        </ul>
      </div>
      <div class="card be">
        <h4>⚙️ 后端负责什么</h4>
        <ul class="feature-list">
          <li>业务规则校验（数据一致性、业务约束）</li>
          <li>数据库的增删改查操作</li>
          <li>事务管理与数据一致性保证</li>
          <li>流程引擎的执行与状态流转</li>
          <li>判读规则的计算与逻辑判定</li>
          <li>配置版本的生成、对比与回滚</li>
          <li>AI 服务调用与结果适配</li>
          <li>数据导入导出的核心处理逻辑</li>
        </ul>
      </div>
    </div>

    <div class="callout">
      <strong>协作原则：</strong>
      前端通过 Service 接口调用后端，后端通过返回值和 Qt 信号通知前端。
      前端<strong>绝不直接操作数据库</strong>，后端<strong>绝不操作UI控件</strong>。
      数据实体（Entity）在后端定义，前端使用数据传输对象（DTO）或直接使用 Entity 的只读访问。
    </div>
  </section>

  <!-- 前端功能详情 -->
  <section>
    <h2 class="fe"><span class="num">1</span>前端功能详细设计</h2>

    <div class="tabs">
      <button class="tab-btn active" onclick="switchTab('fe-main')">主框架</button>
      <button class="tab-btn" onclick="switchTab('fe-module-a')">参数配置模块</button>
      <button class="tab-btn" onclick="switchTab('fe-module-b')">流程编排模块</button>
      <button class="tab-btn" onclick="switchTab('fe-module-c')">判读配置模块</button>
      <button class="tab-btn" onclick="switchTab('fe-common')">通用组件</button>
    </div>

    <!-- 主框架 -->
    <div id="fe-main" class="tab-content active">
      <h3 class="fe">1.1 主窗口框架</h3>

      <div class="wireframe">
        <div class="wf-title">主窗口布局示意</div>
        <div class="wf-toolbar">
          <span class="wf-btn">新建</span>
          <span class="wf-btn">打开</span>
          <span class="wf-btn">保存</span>
          <span class="wf-btn sec">导入</span>
          <span class="wf-btn sec">导出</span>
          <span style="flex:1"></span>
          <span class="wf-btn sec">用户</span>
        </div>
        <div class="wf-body">
          <div class="wf-sidebar">
            左侧导航栏<br><br>
            📊 数据块配置<br>
            🔀 流程编排<br>
            ✅ 判读配置<br>
            📋 系统管理
          </div>
          <div class="wf-main">
            中央工作区（多标签页）<br><br>
            [数据块编辑] [流程画布] [判读规则] ...
          </div>
          <div class="wf-props">
            右侧属性面板<br><br>
            当前选中项<br>属性编辑...<br><br>
            AI 建议区
          </div>
        </div>
      </div>

      <div class="card-grid">
        <div class="card fe">
          <h4>主窗口 (MainWindow)</h4>
          <ul class="feature-list fe-list">
            <li>菜单栏：文件、编辑、视图、工具、帮助</li>
            <li>工具栏：常用操作快捷按钮</li>
            <li>状态栏：当前状态、进度提示、连接状态</li>
            <li>左侧导航栏：模块切换</li>
            <li>中央工作区：多标签页（QTabWidget / QMdiArea）</li>
            <li>右侧属性面板：上下文相关的属性编辑</li>
            <li>底部日志输出窗口</li>
          </ul>
        </div>
        <div class="card fe">
          <h4>导航与工作区</h4>
          <ul class="feature-list fe-list">
            <li>QTreeWidget 左侧导航树，按模块分组</li>
            <li>点击导航项在中央区打开对应标签页</li>
            <li>支持多标签同时打开，标签间拖拽排序</li>
            <li>标签页关闭前检查未保存修改</li>
            <li>最近打开记录（配置在本地 QSettings）</li>
            <li>支持标签页分屏显示（左右/上下对比）</li>
          </ul>
        </div>
      </div>

      <h4>核心类</h4>
      <div class="two-col">
        <div class="class-box">
          <div class="class-name">MainWindow : public QMainWindow</div>
          <div class="class-section">
            <div class="section-label">主要成员</div>
            <ul>
              <li><span class="pub">+</span> setupUi() 初始化界面</li>
              <li><span class="pub">+</span> openModule(moduleId) 打开模块</li>
              <li><span class="pub">+</span> showPropertyPanel(widget) 显示属性面板</li>
              <li><span class="pub">+</span> updateStatusBar(text) 更新状态栏</li>
              <li><span class="pri">-</span> m_navTree 导航树</li>
              <li><span class="pri">-</span> m_tabWidget 标签页</li>
              <li><span class="pri">-</span> m_propertyDock 属性面板</li>
            </ul>
          </div>
        </div>
        <div class="class-box">
          <div class="class-name">NavTreeWidget : public QTreeWidget</div>
          <div class="class-section">
            <div class="section-label">主要成员</div>
            <ul>
              <li><span class="pub">+</span> buildNavTree() 构建导航树</li>
              <li><span class="pub">+</span> currentModuleId() 当前选中模块</li>
              <li><span class="pub">+</span> moduleClicked(id) 信号</li>
              <li><span class="pri">-</span> m_moduleItems 模块项映射</li>
            </ul>
          </div>
        </div>
      </div>
    </div>

    <!-- 模块A：参数配置 -->
    <div id="fe-module-a" class="tab-content">
      <h3 class="fe">1.2 参数配置模块</h3>

      <p>参数配置模块以 <strong>7 级树形结构</strong> 组织总线通信参数：总线类别 → 总线 → 设备 → 接口 → 路由 → 数据块 → 参数，
        提供树形导航 + 属性面板的经典编辑模式。</p>

      <div class="wireframe">
        <div class="wf-title">参数配置对话框布局（ParamConfigDialog）</div>
        <div class="wf-toolbar">
          <span class="wf-btn">导入</span>
          <span class="wf-btn">导出</span>
          <span style="flex:1"></span>
          <span class="wf-btn sec">搜索...</span>
        </div>
        <div class="wf-body">
          <div class="wf-sidebar">
            <strong>左侧（垂直分割）</strong><br><br>
            ── 上方：参数树 ──<br>
            📁 总线类别<br>
            └ 🌐 总线<br>
            &nbsp;&nbsp;└ 💻 设备<br>
            &nbsp;&nbsp;&nbsp;&nbsp;└ 🔗 接口<br>
            &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;└ ➡️ 路由<br>
            &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;└ 📄 数据块<br>
            &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;└ 📊 参数<br><br>
            [新增] [删除]<br><br>
            ── 下方：属性面板 ──<br>
            | 属性 | 值 |<br>
            | 节点类型 | 总线 |<br>
            | 名称 | xxx |<br>
            | ... | ... |<br>
            [保 存]
          </div>
          <div class="wf-main">
            <strong>右侧：信息面板</strong><br><br>
            ┌─────────────────┐<br>
            │ 拓扑图 / 数据块信息 / 参数详情<br>
            │ （QStackedWidget 切换）<br>
            │<br>
            │  拓扑图占位<br>
            │  （设备/接口级连接关系）<br>
            │<br>
            └─────────────────┘
          </div>
        </div>
      </div>

      <h4>布局结构</h4>
      <div class="card fe">
        <ul class="feature-list fe-list">
          <li><strong>ParamConfigDialog</strong>：QDialog 容器，使用 QSplitter 嵌套布局</li>
          <li><strong>左右比例 2:3</strong>：左侧为树 + 属性，右侧为信息面板</li>
          <li><strong>左侧垂直分割 3:2</strong>：上方 ParamTree（树视图），下方属性面板（QStackedWidget）</li>
          <li><strong>属性面板切换</strong>：参数节点 → ParamPropertyPanel，非参数节点 → NodePropertyPanel，多选/无选中 → 空白占位</li>
          <li><strong>右侧信息面板</strong>：拓扑图 / 数据块信息 / 参数详情（QStackedWidget 切换）</li>
        </ul>
      </div>

      <h4>功能清单</h4>
      <div class="table-wrap">
        <table>
          <thead class="fe">
            <tr><th>功能点</th><th>说明</th><th>交互方式</th></tr>
          </thead>
          <tbody>
            <tr>
              <td><strong>7级树形导航</strong></td>
              <td>QTreeView + QStandardItemModel，7级层级：总线类别→总线→设备→接口→路由→数据块→参数</td>
              <td>单击选中节点，双击展开/折叠（不编辑），默认全部折叠</td>
            </tr>
            <tr>
              <td><strong>节点类型标签</strong></td>
              <td>每个节点存储 NodeType 枚举（NodeCategory/NodeBus/NodeDevice/NodeInterface/NodeRoute/NodeDatablock/NodeParameter）</td>
              <td>通过 UserRole 存储，属性面板根据类型切换</td>
            </tr>
            <tr>
              <td><strong>多选支持</strong></td>
              <td>ExtendedSelection 模式，支持 Ctrl/Shift 多选节点</td>
              <td>多选时属性面板显示空白占位</td>
            </tr>
            <tr>
              <td><strong>节点图标</strong></td>
              <td>7个 SVG 扁平风格图标：蓝文件夹(类别)/青网络(总线)/绿设备(设备)/橙链接(接口)/紫箭头(路由)/青文档(数据块)/红柱状图(参数)</td>
              <td>资源文件加载，按节点类型分配</td>
            </tr>
            <tr>
              <td><strong>新增节点</strong></td>
              <td>右键菜单或工具栏按钮，根据当前选中节点类型自动创建对应子节点</td>
              <td>右键 → 新增子节点 / 工具栏新增按钮</td>
            </tr>
            <tr>
              <td><strong>删除节点</strong></td>
              <td>级联删除所有子节点，删除前确认提示</td>
              <td>右键 → 删除 / Delete 键 / 工具栏删除按钮</td>
            </tr>
            <tr>
              <td><strong>节点不可编辑</strong></td>
              <td>树节点名称不可直接编辑，只能在下方属性面板修改后保存</td>
              <td>NoEditTriggers + 属性面板保存按钮</td>
            </tr>
            <tr>
              <td><strong>NodePropertyPanel</strong></td>
              <td>6种非参数节点属性面板：QTableWidget 两列（属性|值），支持下拉框，底部保存按钮</td>
              <td>根据节点类型动态生成属性行</td>
            </tr>
            <tr>
              <td><strong>ParamPropertyPanel</strong></td>
              <td>参数级详细表单：基本信息/位信息/数值属性/计算公式/枚举定义/描述/保存按钮</td>
              <td>QFormLayout + 各类输入控件 + 枚举表格</td>
            </tr>
            <tr>
              <td><strong>搜索过滤</strong></td>
              <td>搜索框实时过滤树节点</td>
              <td>QLineEdit + 文本匹配过滤</td>
            </tr>
            <tr>
              <td><strong>样式美化</strong></td>
              <td>浅色背景、圆角边框、悬停高亮、选中蓝色、展开/折叠SVG三角箭头</td>
              <td>QSS 样式表定制</td>
            </tr>
          </tbody>
        </table>
      </div>

      <h4>6 种非参数节点属性一览</h4>
      <div class="table-wrap">
        <table>
          <thead class="fe">
            <tr><th>节点类型</th><th>属性字段</th><th>特殊控件</th></tr>
          </thead>
          <tbody>
            <tr>
              <td><strong>总线类别</strong></td>
              <td>节点类型（只读）、名称、描述、排序</td>
              <td>—</td>
            </tr>
            <tr>
              <td><strong>总线</strong></td>
              <td>节点类型（只读）、名称、描述、总线类型、协议版本、最大数据块长度</td>
              <td>—</td>
            </tr>
            <tr>
              <td><strong>设备</strong></td>
              <td>节点类型（只读）、名称、描述、设备类型、所属总线（只读）</td>
              <td>—</td>
            </tr>
            <tr>
              <td><strong>接口</strong></td>
              <td>节点类型（只读）、名称、接口描述、适配器、配置、宿主IP、硬件地址、是否自动启动、所属总线（只读）</td>
              <td>—</td>
            </tr>
            <tr>
              <td><strong>路由</strong></td>
              <td>节点类型（只读）、名称、方向、处理方式、周期(ms)、描述</td>
              <td>方向（下拉：发送/接收/双向）、处理方式（下拉：周期/事件/DMA/中断）</td>
            </tr>
            <tr>
              <td><strong>数据块</strong></td>
              <td>节点类型（只读）、块名称、是否指定块长度、长度(字节)、触发模式、描述</td>
              <td>是否指定块长度（下拉：是/否）</td>
            </tr>
          </tbody>
        </table>
      </div>

      <h4>核心类</h4>
      <div class="two-col">
        <div class="class-box">
          <div class="class-name">ParamConfigDialog : public QDialog</div>
          <div class="class-section">
            <div class="section-label">主要成员</div>
            <ul>
              <li><span class="pub">+</span> ParamConfigDialog(parent) 构造</li>
              <li><span class="pri">-</span> buildUi() 构建界面布局</li>
              <li><span class="pri">-</span> buildToolBar() 构建工具栏</li>
              <li><span class="pri">-</span> buildInfoPanels() 构建右侧信息面板</li>
              <li><span class="pri">-</span> switchPropertyPanel(node) 切换属性面板</li>
              <li><span class="pri">-</span> switchInfoPanel(node) 切换信息面板</li>
              <li><span class="pri">-</span> m_paramTree 参数树控件</li>
              <li><span class="pri">-</span> m_propertyStack 属性面板堆叠</li>
              <li><span class="pri">-</span> m_infoStack 信息面板堆叠</li>
            </ul>
          </div>
        </div>
        <div class="class-box">
          <div class="class-name">ParamTree : public QWidget</div>
          <div class="class-section">
            <div class="section-label">主要成员</div>
            <ul>
              <li><span class="pub">+</span> loadFromDatabase() 从数据库加载整棵树</li>
              <li><span class="pub">+</span> currentNode() → TreeNode 获取当前节点</li>
              <li><span class="pub">+</span> nodeSelected(node) 信号</li>
              <li><span class="pri">-</span> m_tree QTreeView 树视图</li>
              <li><span class="pri">-</span> m_model QStandardItemModel 数据模型</li>
              <li><span class="pri">-</span> m_searchEdit 搜索框</li>
              <li><span class="pri">-</span> m_btnAdd / m_btnDelete 工具按钮</li>
              <li><span class="pri">-</span> onAddCategory/Bus/Device/... 新增各类型节点</li>
              <li><span class="pri">-</span> onDeleteNode() 删除节点（级联）</li>
            </ul>
          </div>
        </div>
      </div>
      <div class="two-col" style="margin-top:0.6rem">
        <div class="class-box">
          <div class="class-name">NodePropertyPanel : public QWidget</div>
          <div class="class-section">
            <div class="section-label">主要成员</div>
            <ul>
              <li><span class="pub">+</span> loadNode(node) 加载节点属性</li>
              <li><span class="pub">+</span> clearForm() 清空表单</li>
              <li><span class="pri">-</span> m_table QTableWidget 两列表格</li>
              <li><span class="pri">-</span> m_btnSave 保存按钮</li>
              <li><span class="pri">-</span> addRow(prop, value, editable) 添加属性行</li>
              <li><span class="pri">-</span> addRowWithCombo(prop, options, idx) 添加下拉行</li>
              <li><span class="pri">-</span> setupCategory/Bus/Device/Interface/Route/Datablock(id)</li>
            </ul>
          </div>
        </div>
        <div class="class-box">
          <div class="class-name">ParamPropertyPanel : public QWidget</div>
          <div class="class-section">
            <div class="section-label">主要成员</div>
            <ul>
              <li><span class="pub">+</span> loadParameter(param) 加载参数</li>
              <li><span class="pub">+</span> clearForm() 清空表单</li>
              <li><span class="pub">+</span> parameterChanged(paramId) 信号</li>
              <li><span class="pri">-</span> m_editName / m_editCode 名称编码</li>
              <li><span class="pri">-</span> m_cmbType / m_spinStartBit / m_spinBitWidth 位信息</li>
              <li><span class="pri">-</span> m_cmbByteOrder / m_editUnit / m_spinPrecision 数值属性</li>
              <li><span class="pri">-</span> m_editDefault / m_editMin / m_editMax 值范围</li>
              <li><span class="pri">-</span> m_editFormula 计算公式</li>
              <li><span class="pri">-</span> m_tableEnum 枚举定义表格</li>
              <li><span class="pri">-</span> m_editDesc 描述</li>
              <li><span class="pri">-</span> m_btnSave 保存按钮</li>
            </ul>
          </div>
        </div>
      </div>

      <div class="callout">
        <strong>数据结构 TreeNode：</strong>
        每个树节点封装为 <code>TreeNode</code> 结构体，包含 id、parentId、level、name、code、type(NodeType enum)、userData。
        通过 <code>QStandardItem</code> 的 UserRole 存储，实现数据与视图分离。
      </div>
    </div>

    <!-- 模块B：流程编排 -->
    <div id="fe-module-b" class="tab-content">
      <h3 class="fe">1.3 自动化流程编排模块</h3>

      <p>流程编排模块提供可视化画布，用户通过拖拽方式搭建自动化测试流程。</p>

      <div class="wireframe">
        <div class="wf-title">流程编排画布布局</div>
        <div class="wf-toolbar">
          <span class="wf-btn">保存</span>
          <span class="wf-btn sec">运行</span>
          <span class="wf-btn sec">单步调试</span>
          <span class="wf-btn sec">校验</span>
          <span style="flex:1"></span>
          <span class="wf-btn sec">缩放 + -</span>
          <span class="wf-btn sec">对齐</span>
        </div>
        <div class="wf-body">
          <div class="wf-sidebar">
            指令库面板<br><br>
            ── 基础指令 ──<br>
            ▢ 开始<br>
            ▢ 结束<br>
            ◇ 条件判断<br>
            ↻ 循环<br><br>
            ── 设备指令 ──<br>
            ▢ 发送命令<br>
            ▢ 读取数据<br>
            ▢ 延时等待<br><br>
            拖拽到画布
          </div>
          <div class="wf-main">
            流程画布（Graphics View）<br><br>
            [开始] → [初始化] → [条件?]<br>
                              ↙    ↘<br>
                          [分支A]  [分支B]<br>
                              ↘    ↙<br>
                           [判读] → [结束]<br><br>
            节点可拖拽、连线可编辑
          </div>
          <div class="wf-props">
            节点属性面板<br><br>
            节点名称：xxx<br>
            节点类型：发送命令<br>
            指令：SEND_DATA<br>
            数据块：模拟量输入<br>
            超时：1000ms<br>
            重试次数：3<br><br>
            ── 连线条件 ──<br>
            条件表达式：xxx<br><br>
            💡 AI 建议...
          </div>
        </div>
      </div>

      <h4>功能清单</h4>
      <div class="table-wrap">
        <table>
          <thead class="fe">
            <tr><th>功能点</th><th>说明</th><th>交互方式</th></tr>
          </thead>
          <tbody>
            <tr>
              <td><strong>指令库面板</strong></td>
              <td>左侧分类展示可用指令，支持搜索过滤，拖拽到画布创建节点</td>
              <td>QTreeWidget + 拖拽支持 (QDrag)</td>
            </tr>
            <tr>
              <td><strong>流程画布</strong></td>
              <td>基于 QGraphicsScene/View 的可视化画布，支持缩放、平移、网格对齐</td>
              <td>QGraphicsView 滚轮缩放、右键拖拽平移</td>
            </tr>
            <tr>
              <td><strong>节点操作</strong></td>
              <td>拖拽创建、选中、移动、删除、复制粘贴、框选多选</td>
              <td>鼠标拖拽、快捷键 (Del/Ctrl+C/Ctrl+V)</td>
            </tr>
            <tr>
              <td><strong>连线操作</strong></td>
              <td>从节点输出端口拖到另一节点输入端口创建连线，支持条件连线</td>
              <td>端口 hover 显示连接点，拖拽连线</td>
            </tr>
            <tr>
              <td><strong>节点属性编辑</strong></td>
              <td>右侧属性面板编辑节点参数，不同类型节点显示不同属性</td>
              <td>QFormLayout + 动态属性控件</td>
            </tr>
            <tr>
              <td><strong>流程校验</strong></td>
              <td>检查流程完整性：是否有开始/结束、孤立节点、连线合法性</td>
              <td>校验按钮 + 错误列表弹窗</td>
            </tr>
            <tr>
              <td><strong>运行与调试</strong></td>
              <td>启动流程执行，实时高亮当前节点，显示执行日志，支持断点、单步</td>
              <td>运行工具栏 + 执行状态高亮</td>
            </tr>
            <tr>
              <td><strong>子流程</strong></td>
              <td>支持将一段流程封装为子流程节点，可复用</td>
              <td>选中节点 → 右键 → 封装为子流程</td>
            </tr>
            <tr>
              <td><strong>AI 流程生成</strong></td>
              <td>输入自然语言描述，AI 自动生成流程骨架，用户确认后插入画布</td>
              <td>对话框输入描述 → 预览 → 确认插入</td>
            </tr>
            <tr>
              <td><strong>流程版本管理</strong></td>
              <td>保存版本、版本对比、回滚</td>
              <td>版本列表 + 可视化 Diff</td>
            </tr>
          </tbody>
        </table>
      </div>

      <h4>核心类</h4>
      <div class="two-col">
        <div class="class-box">
          <div class="class-name">FlowCanvasView : public QGraphicsView</div>
          <div class="class-section">
            <div class="section-label">主要方法</div>
            <ul>
              <li><span class="pub">+</span> setZoom(qreal) 设置缩放</li>
              <li><span class="pub">+</span> fitToView() 适应窗口</li>
              <li><span class="pub">+</span> selectedNodes() 选中节点</li>
              <li><span class="pub">+</span> deleteSelected() 删除选中</li>
              <li><span class="pri">-</span> wheelEvent() 滚轮缩放</li>
              <li><span class="pri">-</span> mousePressEvent() 平移/选择</li>
            </ul>
          </div>
        </div>
        <div class="class-box">
          <div class="class-name">FlowNodeItem : public QGraphicsItem</div>
          <div class="class-section">
            <div class="section-label">主要方法</div>
            <ul>
              <li><span class="pub">+</span> nodeId() / nodeType()</li>
              <li><span class="pub">+</span> setSelected(bool)</li>
              <li><span class="pub">+</span> inputPorts() / outputPorts()</li>
              <li><span class="pub">+</span> boundingRect() / paint()</li>
              <li><span class="pri">-</span> m_ports 端口列表</li>
              <li><span class="pri">-</span> m_nodeData 节点数据</li>
            </ul>
          </div>
        </div>
      </div>
      <div class="two-col" style="margin-top:0.6rem">
        <div class="class-box">
          <div class="class-name">FlowEdgeItem : public QGraphicsPathItem</div>
          <div class="class-section">
            <div class="section-label">主要方法</div>
            <ul>
              <li><span class="pub">+</span> setStartPoint(QPointF)</li>
              <li><span class="pub">+</span> setEndPoint(QPointF)</li>
              <li><span class="pub">+</span> sourceNode() / targetNode()</li>
              <li><span class="pub">+</span> updatePath() 贝塞尔曲线</li>
            </ul>
          </div>
        </div>
        <div class="class-box">
          <div class="class-name">NodePropertyPanel : public QWidget</div>
          <div class="class-section">
            <div class="section-label">主要方法</div>
            <ul>
              <li><span class="pub">+</span> setNode(nodeId) 绑定节点</li>
              <li><span class="pub">+</span> updateProperties() 刷新属性</li>
              <li><span class="pub">+</span> propertyChanged(key,val) 信号</li>
              <li><span class="pri">-</span> m_formLayout 动态表单</li>
            </ul>
          </div>
        </div>
      </div>
    </div>

    <!-- 模块C：判读配置 -->
    <div id="fe-module-c" class="tab-content">
      <h3 class="fe">1.4 判读参数配置模块</h3>

      <p>判读配置模块用于定义测试结果的判定规则，支持阈值比较、多条件组合和逻辑运算。</p>

      <div class="wireframe">
        <div class="wf-title">判读规则配置界面</div>
        <div class="wf-toolbar">
          <span class="wf-btn">新建规则</span>
          <span class="wf-btn sec">保存</span>
          <span class="wf-btn sec">测试判读</span>
          <span class="wf-btn sec">导入</span>
          <span style="flex:1"></span>
          <span class="wf-btn sec">规则模板</span>
        </div>
        <div class="wf-body">
          <div class="wf-sidebar">
            判读规则列表<br><br>
            • 电压超限判读<br>
            • 通讯状态判读<br>
            • 温度趋势判读<br>
            • 综合判定规则<br>
            • ...
          </div>
          <div class="wf-main">
            规则编辑区（树形结构）<br><br>
            规则组 [AND]<br>
            ├─ 条件1：voltage > 5.0V  →  失败<br>
            ├─ 条件组 [OR]<br>
            │   ├─ 条件2：current > 10A<br>
            │   └─ 条件3：power > 100W<br>
            └─ 条件4：status == OK  →  通过<br><br>
            [添加条件] [添加条件组] [删除] [上移/下移]<br>
            逻辑运算符可切换：AND / OR / NOT
          </div>
          <div class="wf-props">
            条件属性<br><br>
            条件名称：xxx<br>
            数据字段：voltage<br>
            比较符：大于 ><br>
            阈值：5.0<br>
            单位：V<br>
            结果：失败/通过/警告<br><br>
            ── 测试数据 ──<br>
            输入测试值：[___]<br>
            [验证] → 通过/失败<br><br>
            💡 AI 建议阈值
          </div>
        </div>
      </div>

      <h4>功能清单</h4>
      <div class="table-wrap">
        <table>
          <thead class="fe">
            <tr><th>功能点</th><th>说明</th><th>交互方式</th></tr>
          </thead>
          <tbody>
            <tr>
              <td><strong>规则列表管理</strong></td>
              <td>展示所有判读规则，支持按类型/关联数据块筛选</td>
              <td>QListView / QTreeView</td>
            </tr>
            <tr>
              <td><strong>规则树形编辑</strong></td>
              <td>以树形结构编辑条件和条件组，支持嵌套多层</td>
              <td>QTreeWidget + 自定义 Item</td>
            </tr>
            <tr>
              <td><strong>条件配置</strong></td>
              <td>选择字段、比较符(=/!=/&gt;/&lt;/&gt;=/&lt;=/in/range)、阈值、结果类型</td>
              <td>属性面板表单</td>
            </tr>
            <tr>
              <td><strong>逻辑组合</strong></td>
              <td>条件组支持 AND / OR / NOT 逻辑运算，可自由嵌套</td>
              <td>条件组节点 + 逻辑下拉选择</td>
            </tr>
            <tr>
              <td><strong>关联配置</strong></td>
              <td>判读规则关联到指定数据块和流程节点</td>
              <td>下拉选择数据块和流程</td>
            </tr>
            <tr>
              <td><strong>阈值类型</strong></td>
              <td>支持固定值、百分比、范围、表达式多种阈值形式</td>
              <td>阈值类型下拉 + 值输入</td>
            </tr>
            <tr>
              <td><strong>单条验证</strong></td>
              <td>输入测试数据，立即验证规则判定结果</td>
              <td>测试输入框 + 验证按钮 + 结果提示</td>
            </tr>
            <tr>
              <td><strong>判读报告模板</strong></td>
              <td>配置判读报告的输出格式和内容项</td>
              <td>模板编辑器</td>
            </tr>
            <tr>
              <td><strong>AI 判读辅助</strong></td>
              <td>对异常数据给出 AI 分析建议，辅助人工判读</td>
              <td>侧边栏 AI 分析面板</td>
            </tr>
          </tbody>
        </table>
      </div>
    </div>

    <!-- 通用组件 -->
    <div id="fe-common" class="tab-content">
      <h3 class="fe">1.5 通用组件与工具类</h3>

      <div class="card-grid">
        <div class="card fe">
          <h4>基础控件</h4>
          <ul class="feature-list fe-list">
            <li>MessageBox：统一风格的消息框</li>
            <li>InputDialog：标准化输入对话框</li>
            <li>ProgressDialog：带进度的等待框</li>
            <li>Toast：右下角轻量提示</li>
            <li>LoadingOverlay：加载遮罩层</li>
          </ul>
        </div>
        <div class="card fe">
          <h4>编辑控件</h4>
          <ul class="feature-list fe-list">
            <li>NumberEdit：带单位/范围的数值输入框</li>
            <li>EnumComboBox：枚举下拉框</li>
            <li>ColorPicker：颜色选择器</li>
            <li>PathEdit：路径选择框（文件/文件夹）</li>
            <li>JsonEditor：JSON 文本编辑器（高亮）</li>
          </ul>
        </div>
        <div class="card fe">
          <h4>工具类</h4>
          <ul class="feature-list fe-list">
            <li>UiUtil：通用界面工具（居中、缩放等）</li>
            <li>StyleHelper：QSS 样式管理</li>
            <li>ValidatorFactory：校验器工厂</li>
            <li>IconHelper：图标资源管理</li>
          </ul>
        </div>
        <div class="card fe">
          <h4>AI 交互组件</h4>
          <ul class="feature-list fe-list">
            <li>AiSuggestionBubble：AI 建议气泡</li>
            <li>AiSidePanel：AI 助手侧边栏</li>
            <li>AiChatWidget：AI 对话窗口</li>
            <li>LoadingSpinner：AI 思考中动画</li>
          </ul>
        </div>
      </div>
    </div>
  </section>

  <!-- 后端功能详情 -->
  <section>
    <h2 class="be"><span class="num">2</span>后端功能详细设计</h2>

    <div class="tabs">
      <button class="tab-btn be-tab" onclick="switchTab('be-dal')">数据访问层</button>
      <button class="tab-btn be-tab" onclick="switchTab('be-module-a')">参数配置数据层</button>
      <button class="tab-btn be-tab" onclick="switchTab('be-module-b')">流程引擎服务</button>
      <button class="tab-btn be-tab" onclick="switchTab('be-module-c')">判读规则服务</button>
      <button class="tab-btn be-tab" onclick="switchTab('be-common')">公共服务</button>
    </div>

    <!-- 数据访问层 -->
    <div id="be-dal" class="tab-content">
      <h3 class="be">2.1 数据访问层（DAO）</h3>

      <p>
        数据访问层封装所有数据库操作，向上层 Service 提供面向对象的数据访问接口，
        屏蔽 SQL 细节，管理数据库连接和事务。
      </p>

      <h4>核心功能</h4>
      <div class="card-grid">
        <div class="card be">
          <h4>数据库连接池</h4>
          <ul class="feature-list">
            <li>管理多个 QSqlDatabase 连接</li>
            <li>支持连接的获取与归还</li>
            <li>连接有效性检测（心跳）</li>
            <li>最小/最大连接数配置</li>
            <li>线程安全设计</li>
          </ul>
        </div>
        <div class="card be">
          <h4>DAO 基类</h4>
          <ul class="feature-list">
            <li>通用 CRUD 方法封装</li>
            <li>分页查询支持</li>
            <li>条件查询构建器</li>
            <li>事务管理接口</li>
            <li>SQL 日志输出</li>
          </ul>
        </div>
        <div class="card be">
          <h4>实体类（Entity）</h4>
          <ul class="feature-list">
            <li>ParamCategory / ParamBus / ParamDevice</li>
            <li>ParamInterface / ParamRoute / ParamDatablock</li>
            <li>ParamParameter（参数配置7级）</li>
            <li>Flow / FlowNode / FlowEdge（规划中）</li>
            <li>JudgeRule / JudgeCondition（规划中）</li>
            <li>每个实体对应一张数据库表</li>
          </ul>
        </div>
        <div class="card be">
          <h4>数据库迁移</h4>
          <ul class="feature-list">
            <li>版本化管理 schema 变更</li>
            <li>自动检测并执行升级脚本</li>
            <li>初始化基础数据（字典等）</li>
            <li>数据库版本信息表</li>
          </ul>
        </div>
      </div>

      <h4>类结构</h4>
      <div class="two-col">
        <div class="class-box be">
          <div class="class-name">DbConnectionPool</div>
          <div class="class-section">
            <div class="section-label">主要方法</div>
            <ul>
              <li><span class="pub">+</span> static instance() 单例</li>
              <li><span class="pub">+</span> init(config) 初始化连接池</li>
              <li><span class="pub">+</span> acquire() 获取连接</li>
              <li><span class="pub">+</span> release(conn) 归还连接</li>
              <li><span class="pub">+</span> testConnection(conn) 检测连接</li>
              <li><span class="pri">-</span> m_availableConnections 可用连接队列</li>
              <li><span class="pri">-</span> m_mutex 互斥锁</li>
            </ul>
          </div>
        </div>
        <div class="class-box be">
          <div class="class-name">BaseDao&lt;T&gt;</div>
          <div class="class-section">
            <div class="section-label">主要方法</div>
            <ul>
              <li><span class="pub">+</span> insert(entity) → id</li>
              <li><span class="pub">+</span> update(entity) → bool</li>
              <li><span class="pub">+</span> remove(id) → bool</li>
              <li><span class="pub">+</span> findById(id) → T</li>
              <li><span class="pub">+</span> findAll() → QList&lt;T&gt;</li>
              <li><span class="pub">+</span> findByCondition(cond) → QList&lt;T&gt;</li>
              <li><span class="pub">+</span> count() → int</li>
              <li><span class="pub">+</span> transaction() / commit() / rollback()</li>
            </ul>
          </div>
        </div>
      </div>

      <h4>各模块 DAO 一览</h4>
      <div class="table-wrap">
        <table>
          <thead class="be">
            <tr><th>DAO 类</th><th>对应表</th><th>特有方法</th></tr>
          </thead>
          <tbody>
            <tr>
              <td class="code-col">ParamDatabase</td>
              <td>bus_category / bus / bus_device / bus_interface / bus_route / bus_datablock / bus_parameter</td>
              <td>7级层级完整CRUD、nameExistsForType() 名称重名检查、countByParent() 子节点计数、createTables() 建表、级联删除</td>
            </tr>
            <tr>
              <td class="code-col">FlowDao</td>
              <td>t_flow</td>
              <td>findDetail(id) 含节点和连线 / copyFlow(id)（规划中）</td>
            </tr>
            <tr>
              <td class="code-col">FlowNodeDao</td>
              <td>t_flow_node</td>
              <td>findByFlowId(flowId) / deleteByFlowId(flowId)（规划中）</td>
            </tr>
            <tr>
              <td class="code-col">FlowEdgeDao</td>
              <td>t_flow_edge</td>
              <td>findByFlowId(flowId) / deleteByFlowId(flowId)（规划中）</td>
            </tr>
            <tr>
              <td class="code-col">JudgeRuleDao</td>
              <td>t_judge_rule</td>
              <td>findDetail(id) 含条件树 / findByBlockId(blockId)（规划中）</td>
            </tr>
            <tr>
              <td class="code-col">JudgeConditionDao</td>
              <td>t_judge_condition</td>
              <td>findTreeByRuleId(ruleId) / deleteByRuleId(ruleId)（规划中）</td>
            </tr>
            <tr>
              <td class="code-col">ConfigVersionDao</td>
              <td>t_config_version</td>
              <td>saveSnapshot(type,id,json) / listVersions(type,id) / getVersion(type,id,ver)（规划中）</td>
            </tr>
            <tr>
              <td class="code-col">SysDictDao</td>
              <td>t_sys_dict</td>
              <td>getDictItems(dictType) / getAllDicts()（规划中）</td>
            </tr>
          </tbody>
        </table>
      </div>
    </div>

    <!-- 模块A：参数配置数据层 -->
    <div id="be-module-a" class="tab-content">
      <h3 class="be">2.2 参数配置数据层（模块 A）</h3>

      <p>参数配置数据层由 <code>ParamDatabase</code> 类统一封装，负责 7 张参数配置表的所有数据库操作，
        向上层提供面向对象的数据访问接口，屏蔽 SQL 细节。</p>

      <h4>功能清单</h4>
      <div class="table-wrap">
        <table>
          <thead class="be">
            <tr><th>功能</th><th>说明</th><th>业务规则</th></tr>
          </thead>
          <tbody>
            <tr>
              <td><strong>建表与检测</strong></td>
              <td>程序启动时自动检测并创建 7 张参数配置表</td>
              <td>createTables() 逐表检测，不存在则创建；tableExists() 检测单表是否存在</td>
            </tr>
            <tr>
              <td><strong>7 级层级 CRUD</strong></td>
              <td>总线类别、总线、设备、接口、路由、数据块、参数 各有完整的增删改查</td>
              <td>每级 listXxx(parentId) / addXxx() / updateXxx() / deleteXxx() 四件套</td>
            </tr>
            <tr>
              <td><strong>名称唯一性检查</strong></td>
              <td>同类型同级节点名称不可重复</td>
              <td>nameExistsForType(tableName, name, excludeId) 通用方法，新增和更新时调用</td>
            </tr>
            <tr>
              <td><strong>级联删除</strong></td>
              <td>删除父节点时自动删除所有子节点及其后代</td>
              <td>删除类别 → 删除其下所有总线/设备/接口/路由/数据块/参数</td>
            </tr>
            <tr>
              <td><strong>子节点计数</strong></td>
              <td>统计指定父节点下的子节点数量</td>
              <td>countByParent(table, parentColumn, parentId) 通用方法</td>
            </tr>
            <tr>
              <td><strong>保留字处理</strong></td>
              <td>MySQL 保留字字段的 SQL 转义</td>
              <td>precision 字段使用反引号 `precision` 转义</td>
            </tr>
            <tr>
              <td><strong>时间戳</strong></td>
              <td>所有表均有 created_at / updated_at 字段</td>
              <td>新增时写入 created_at，更新时刷新 updated_at</td>
            </tr>
          </tbody>
        </table>
      </div>

      <h4>核心类</h4>
      <div class="class-box be">
        <div class="class-name">ParamDatabase : public QObject</div>
        <div class="class-section">
          <div class="section-label">公共接口（供前端/服务层调用）</div>
          <ul>
            <li><span class="pub">+</span> createTables(err) → bool 建表</li>
            <li><span class="pub">+</span> tableExists(tableName) → bool 表存在检测</li>
            <li><span class="pub">+</span> nameExistsForType(table, name, excludeId) → bool 名称重名检查</li>
            <li><span class="pub">+</span> countByParent(table, parentCol, parentId) → int 子节点计数</li>
            <li><span class="pub">+</span> listCategories(err) → QVector&lt;ParamCategory&gt;</li>
            <li><span class="pub">+</span> addCategory(cat, err) → int</li>
            <li><span class="pub">+</span> updateCategory(cat, err) → bool</li>
            <li><span class="pub">+</span> deleteCategory(id, err) → bool</li>
            <li><span class="pub">+</span> listBuses(categoryId, err) → QVector&lt;ParamBus&gt;</li>
            <li><span class="pub">+</span> addBus(bus, err) → int</li>
            <li><span class="pub">+</span> updateBus(bus, err) → bool</li>
            <li><span class="pub">+</span> deleteBus(id, err) → bool</li>
            <li><span class="pub">+</span> listDevices(busId, err) → QVector&lt;ParamDevice&gt;</li>
            <li><span class="pub">+</span> addDevice(dev, err) → int</li>
            <li><span class="pub">+</span> updateDevice(dev, err) → bool</li>
            <li><span class="pub">+</span> deleteDevice(id, err) → bool</li>
            <li><span class="pub">+</span> listInterfaces(deviceId, err) → QVector&lt;ParamInterface&gt;</li>
            <li><span class="pub">+</span> addInterface(iface, err) → int</li>
            <li><span class="pub">+</span> updateInterface(iface, err) → bool</li>
            <li><span class="pub">+</span> deleteInterface(id, err) → bool</li>
            <li><span class="pub">+</span> listRoutes(interfaceId, err) → QVector&lt;ParamRoute&gt;</li>
            <li><span class="pub">+</span> addRoute(route, err) → int</li>
            <li><span class="pub">+</span> updateRoute(route, err) → bool</li>
            <li><span class="pub">+</span> deleteRoute(id, err) → bool</li>
            <li><span class="pub">+</span> listDatablocks(routeId, err) → QVector&lt;ParamDatablock&gt;</li>
            <li><span class="pub">+</span> addDatablock(blk, err) → int</li>
            <li><span class="pub">+</span> updateDatablock(blk, err) → bool</li>
            <li><span class="pub">+</span> deleteDatablock(id, err) → bool</li>
            <li><span class="pub">+</span> listParameters(datablockId, err) → QVector&lt;ParamParameter&gt;</li>
            <li><span class="pub">+</span> addParameter(param, err) → int</li>
            <li><span class="pub">+</span> updateParameter(param, err) → bool</li>
            <li><span class="pub">+</span> deleteParameter(id, err) → bool</li>
          </ul>
        </div>
      </div>

      <h4>7 个数据结构体</h4>
      <div class="table-wrap">
        <table>
          <thead class="be">
            <tr><th>结构体</th><th>关键字段</th><th>对应表</th></tr>
          </thead>
          <tbody>
            <tr>
              <td class="code-col">ParamCategory</td>
              <td>id, name, description, sortOrder</td>
              <td>bus_category</td>
            </tr>
            <tr>
              <td class="code-col">ParamBus</td>
              <td>id, categoryId, name, code, busType, protocolVersion, description</td>
              <td>bus</td>
            </tr>
            <tr>
              <td class="code-col">ParamDevice</td>
              <td>id, busId, name, code, deviceType, description</td>
              <td>bus_device</td>
            </tr>
            <tr>
              <td class="code-col">ParamInterface</td>
              <td>id, deviceId, name, code, ifType, adapterName, adapterIp, port, baudrate, deviceAddress, description</td>
              <td>bus_interface</td>
            </tr>
            <tr>
              <td class="code-col">ParamRoute</td>
              <td>id, interfaceId, name, direction(1发/2收/3双), processMode(1周期/2事件/3DMA/4中断), periodMs, description</td>
              <td>bus_route</td>
            </tr>
            <tr>
              <td class="code-col">ParamDatablock</td>
              <td>id, routeId, name, code, dataLength, triggerMode, description, sortOrder</td>
              <td>bus_datablock</td>
            </tr>
            <tr>
              <td class="code-col">ParamParameter</td>
              <td>id, datablockId, name, code, paramType, startBit, bitWidth, byteOrder, unit, precision, defaultValue, minValue, maxValue, formula, enumOptions(JSON), description, sortOrder</td>
              <td>bus_parameter</td>
            </tr>
          </tbody>
        </table>
      </div>

      <div class="callout warning">
        <strong>注意事项：</strong>
        <ul>
          <li><code>ParamDatabase</code> 直接继承 QObject，不使用泛型 DAO 基类，7 张表的操作直接在类内实现</li>
          <li>所有方法返回值：新增返回新记录 ID，更新/删除返回 bool，查询返回 QVector&lt;T&gt;</li>
          <li>所有方法支持可选的错误输出参数 <code>QString *err</code>，失败时填充错误信息</li>
          <li>数据库连接通过 <code>DbManager::instance().database()</code> 获取</li>
        </ul>
      </div>
    </div>

    <!-- 模块B：流程引擎 -->
    <div id="be-module-b" class="tab-content">
      <h3 class="be">2.3 流程引擎服务（模块 B）</h3>

      <p>流程引擎服务负责流程定义的管理和流程的执行调度，是整个自动化能力的核心。</p>

      <h4>功能清单</h4>
      <div class="table-wrap">
        <table>
          <thead class="be">
            <tr><th>功能</th><th>说明</th><th>业务规则</th></tr>
          </thead>
          <tbody>
            <tr>
              <td><strong>流程定义 CRUD</strong></td>
              <td>保存、加载、更新、删除流程定义（含节点和连线）</td>
              <td>保存时做完整性校验；版本化管理</td>
            </tr>
            <tr>
              <td><strong>流程校验</strong></td>
              <td>检查流程定义的合法性</td>
              <td>必须有且仅有一个开始节点；必须有结束节点；无孤立节点；连线合法；无死循环</td>
            </tr>
            <tr>
              <td><strong>指令库管理</strong></td>
              <td>注册、查询、分类管理原子指令</td>
              <td>每个指令有唯一标识、参数定义、执行器</td>
            </tr>
            <tr>
              <td><strong>流程执行</strong></td>
              <td>启动、暂停、继续、停止流程</td>
              <td>异步执行（独立线程）；通过信号通知状态变化；支持断点和单步</td>
            </tr>
            <tr>
              <td><strong>节点执行器</strong></td>
              <td>各类节点的执行逻辑</td>
              <td>开始/结束节点；指令节点；条件分支；循环节点；子流程节点；延时节点</td>
            </tr>
            <tr>
              <td><strong>执行上下文</strong></td>
              <td>流程执行过程中的数据容器</td>
              <td>存储数据块实例、中间变量、节点输出结果；各节点可读写</td>
            </tr>
            <tr>
              <td><strong>条件表达式求值</strong></td>
              <td>计算条件分支的表达式</td>
              <td>支持简单表达式（字段比较）；支持 AND/OR 组合；引用上下文中的数据</td>
            </tr>
            <tr>
              <td><strong>异常处理</strong></td>
              <td>执行过程中的错误处理</td>
              <td>节点级重试；流程级异常捕获；错误日志记录；失败回调</td>
            </tr>
            <tr>
              <td><strong>执行日志</strong></td>
              <td>记录流程执行的详细过程</td>
              <td>每个节点的开始/结束时间、输入输出、状态、错误信息</td>
            </tr>
            <tr>
              <td><strong>子流程支持</strong></td>
              <td>流程嵌套调用</td>
              <td>子流程独立上下文；参数传递；返回值接收</td>
            </tr>
          </tbody>
        </table>
      </div>

      <h4>核心类</h4>
      <div class="two-col">
        <div class="class-box be">
          <div class="class-name">FlowDefinitionService</div>
          <div class="class-section">
            <div class="section-label">流程定义管理</div>
            <ul>
              <li><span class="pub">+</span> saveFlow(FlowDefinition) → qint64</li>
              <li><span class="pub">+</span> loadFlow(flowId) → FlowDefinitionPtr</li>
              <li><span class="pub">+</span> deleteFlow(flowId) → bool</li>
              <li><span class="pub">+</span> listFlows(filter) → QList&lt;FlowSummary&gt;</li>
              <li><span class="pub">+</span> validateFlow(flowId) → ValidationResult</li>
              <li><span class="pub">+</span> copyFlow(flowId) → qint64</li>
              <li><span class="pub">+</span> listVersions(flowId) → QList&lt;ConfigVersion&gt;</li>
            </ul>
          </div>
        </div>
        <div class="class-box be">
          <div class="class-name">FlowEngine : public QObject</div>
          <div class="class-section">
            <div class="section-label">流程执行引擎</div>
            <ul>
              <li><span class="pub">+</span> startFlow(flowId, context) → executionId</li>
              <li><span class="pub">+</span> pauseFlow(executionId)</li>
              <li><span class="pub">+</span> resumeFlow(executionId)</li>
              <li><span class="pub">+</span> stopFlow(executionId)</li>
              <li><span class="pub">+</span> stepFlow(executionId) 单步执行</li>
              <li><span class="pub">+</span> getExecutionStatus(executionId) → ExecutionStatus</li>
              <li><span class="pri">-</span> m_executions 执行实例表</li>
            </ul>
          </div>
          <div class="class-section">
            <div class="section-label">信号</div>
            <ul>
              <li>flowStarted(executionId, flowId)</li>
              <li>nodeStarted(executionId, nodeId)</li>
              <li>nodeFinished(executionId, nodeId, success, result)</li>
              <li>flowFinished(executionId, success)</li>
              <li>flowError(executionId, errorMsg)</li>
            </ul>
          </div>
        </div>
      </div>

      <h4>节点执行器体系</h4>
      <figure class="diagram">
        <pre class="mermaid">
classDiagram
    class INodeExecutor {
        <<interface>>
        +execute(node, context) ExecuteResult
        +nodeType() QString
    }
    class StartExecutor {
        +nodeType() "start"
    }
    class EndExecutor {
        +nodeType() "end"
    }
    class InstructionExecutor {
        +nodeType() "instruction"
        +executeInstruction()
    }
    class ConditionExecutor {
        +nodeType() "condition"
        +evaluateExpression()
    }
    class LoopExecutor {
        +nodeType() "loop"
        +executeLoop()
    }
    class SubflowExecutor {
        +nodeType() "subflow"
        +invokeSubflow()
    }
    class DelayExecutor {
        +nodeType() "delay"
    }
    INodeExecutor <|.. StartExecutor
    INodeExecutor <|.. EndExecutor
    INodeExecutor <|.. InstructionExecutor
    INodeExecutor <|.. ConditionExecutor
    INodeExecutor <|.. LoopExecutor
    INodeExecutor <|.. SubflowExecutor
    INodeExecutor <|.. DelayExecutor
        </pre>
        <figcaption>图 2-1：节点执行器类图</figcaption>
      </figure>
    </div>

    <!-- 模块C：判读服务 -->
    <div id="be-module-c" class="tab-content">
      <h3 class="be">2.4 判读规则服务（模块 C）</h3>

      <p>判读规则服务负责判读规则的管理和判定计算，将测试数据与规则进行匹配，输出判读结果。</p>

      <h4>功能清单</h4>
      <div class="table-wrap">
        <table>
          <thead class="be">
            <tr><th>功能</th><th>说明</th><th>业务规则</th></tr>
          </thead>
          <tbody>
            <tr>
              <td><strong>规则 CRUD</strong></td>
              <td>创建、查询、更新、删除判读规则（含条件树）</td>
              <td>保存时校验条件树合法性；版本化管理</td>
            </tr>
            <tr>
              <td><strong>条件树校验</strong></td>
              <td>校验条件配置的正确性</td>
              <td>字段必须存在于关联数据块；比较符与字段类型兼容；阈值有效</td>
            </tr>
            <tr>
              <td><strong>规则判定执行</strong></td>
              <td>输入测试数据，输出判读结果</td>
              <td>递归遍历条件树；短路求值（AND 遇假停止，OR 遇真停止）；返回每条条件的详细结果</td>
            </tr>
            <tr>
              <td><strong>比较运算</strong></td>
              <td>各种比较符的计算</td>
              <td>= / != / > / < / >= / <= / in / not_in / range / contains / starts_with / ends_with</td>
            </tr>
            <tr>
              <td><strong>逻辑运算</strong></td>
              <td>条件组的逻辑组合</td>
              <td>AND（全部满足）/ OR（任一满足）/ NOT（取反）/ XOR（异或）</td>
            </tr>
            <tr>
              <td><strong>结果分级</strong></td>
              <td>判读结果的分级输出</td>
              <td>通过 / 警告 / 失败 / 异常；支持优先级；支持自定义结果级别</td>
            </tr>
            <tr>
              <td><strong>单条验证</strong></td>
              <td>对单个条件进行测试验证</td>
              <td>输入测试值立即得到判定结果，用于配置时的即时验证</td>
            </tr>
            <tr>
              <td><strong>判读报告生成</strong></td>
              <td>生成结构化的判读报告</td>
              <td>包含总结果、各条件明细、不通过项汇总、执行时间</td>
            </tr>
          </tbody>
        </table>
      </div>

      <h4>核心类</h4>
      <div class="two-col">
        <div class="class-box be">
          <div class="class-name">JudgeRuleService</div>
          <div class="class-section">
            <div class="section-label">规则管理</div>
            <ul>
              <li><span class="pub">+</span> saveRule(JudgeRuleWithTree) → qint64</li>
              <li><span class="pub">+</span> getRuleDetail(ruleId) → JudgeRuleWithTree</li>
              <li><span class="pub">+</span> deleteRule(ruleId) → bool</li>
              <li><span class="pub">+</span> listRules(filter) → QList&lt;JudgeRuleSummary&gt;</li>
              <li><span class="pub">+</span> validateRule(ruleId) → ValidationResult</li>
              <li><span class="pub">+</span> listVersions(ruleId) → QList&lt;ConfigVersion&gt;</li>
            </ul>
          </div>
        </div>
        <div class="class-box be">
          <div class="class-name">JudgeEngine</div>
          <div class="class-section">
            <div class="section-label">判读执行</div>
            <ul>
              <li><span class="pub">+</span> evaluate(ruleId, testData) → JudgeResult</li>
              <li><span class="pub">+</span> evaluateCondition(condId, testData) → bool</li>
              <li><span class="pub">+</span> evaluateExpression(expr, context) → bool</li>
              <li><span class="pub">+</span> generateReport(ruleId, testData) → JudgeReport</li>
              <li><span class="pri">-</span> evaluateNode(node, data) 递归求值</li>
              <li><span class="pri">-</span> m_comparators 比较器映射</li>
            </ul>
          </div>
        </div>
      </div>

      <h4>判读结果结构</h4>
      <pre><code>struct JudgeResult {
    bool passed;                 // 总体是否通过
    QString resultLevel;         // 结果级别：PASS/WARNING/FAIL/ERROR
    QString summary;             // 结果摘要
    QList&lt;ConditionResult&gt; details; // 每条条件的判定详情
    QDateTime executeTime;       // 执行时间
    int durationMs;              // 耗时（毫秒）
};

struct ConditionResult {
    qint64 conditionId;
    QString conditionName;
    bool passed;
    QString actualValue;         // 实际值
    QString expectedValue;       // 期望值/阈值
    QString operatorSymbol;      // 比较符
    QString errorMsg;            // 失败原因
};</code></pre>
    </div>

    <!-- 公共服务 -->
    <div id="be-common" class="tab-content">
      <h3 class="be">2.5 公共服务</h3>

      <div class="card-grid">
        <div class="card be">
          <h4>版本管理服务</h4>
          <ul class="feature-list">
            <li>统一的版本号生成策略</li>
            <li>配置快照的序列化与存储</li>
            <li>版本差异对比（Diff）算法</li>
            <li>版本回滚（基于快照重建）</li>
            <li>版本备注与标签</li>
          </ul>
        </div>
        <div class="card be">
          <h4>系统字典服务</h4>
          <ul class="feature-list">
            <li>字典项的增删改查</li>
            <li>按类型获取字典列表</li>
            <li>字典级联（父子关系）</li>
            <li>字典缓存（减少 DB 查询）</li>
            <li>初始化默认字典数据</li>
          </ul>
        </div>
        <div class="card be">
          <h4>日志服务</h4>
          <ul class="feature-list">
            <li>分级日志（Debug/Info/Warn/Error）</li>
            <li>日志文件滚动（按大小/按天）</li>
            <li>操作审计日志</li>
            <li>日志查询界面</li>
            <li>qInstallMessageHandler 接管</li>
          </ul>
        </div>
        <div class="card be">
          <h4>AI 插件管理服务</h4>
          <ul class="feature-list">
            <li>插件目录扫描与加载</li>
            <li>插件元数据读取</li>
            <li>插件启停管理</li>
            <li>AI 服务 HTTP 调用封装</li>
            <li>超时/重试/熔断机制</li>
            <li>建议记录与反馈收集</li>
          </ul>
        </div>
      </div>
    </div>
  </section>

  <!-- 协作接口 -->
  <section>
    <h2><span class="num">3</span>前后端协作接口</h2>

    <h3>3.1 调用关系</h3>
    <figure class="diagram">
      <pre class="mermaid">
sequenceDiagram
    participant UI as 前端 UI Widget
    participant SVC as 后端 Service
    participant DAO as DAO 层
    participant DB as MySQL

    UI->>SVC: 调用 Service 方法（同步/异步）
    SVC->>SVC: 业务校验与逻辑处理
    SVC->>DAO: 调用 DAO 数据操作
    DAO->>DB: 执行 SQL
    DB-->>DAO: 返回结果集
    DAO-->>SVC: 返回 Entity/DTO
    SVC-->>UI: 返回结果 / 发出信号
    UI->>UI: 更新界面显示
      </pre>
      <figcaption>图 3-1：前后端调用时序</figcaption>
    </figure>

    <h3>3.2 同步与异步调用约定</h3>
    <div class="table-wrap">
      <table>
        <thead>
          <tr><th>调用方式</th><th>适用场景</th><th>实现方式</th><th>示例</th></tr>
        </thead>
        <tbody>
          <tr>
            <td><strong>同步调用</strong></td>
            <td>简单查询、快速操作</td>
            <td>直接调用 Service 方法，返回值即结果</td>
            <td>getDataBlock(id)、listBlocks()</td>
          </tr>
          <tr>
            <td><strong>异步调用（信号）</strong></td>
            <td>耗时操作、流程执行</td>
            <td>调用方法立即返回，通过信号通知结果</td>
            <td>startFlow() → flowFinished() 信号</td>
          </tr>
          <tr>
            <td><strong>异步调用（回调）</strong></td>
            <td>AI 调用、远程请求</td>
            <td>传入回调函数或使用 QFutureWatcher</td>
            <td>suggestParams(blockId, callback)</td>
          </tr>
        </tbody>
      </table>
    </div>

    <div class="callout warning">
      <strong>线程安全约定：</strong>
      Service 层的方法默认在调用者线程执行。涉及耗时操作的 Service 内部使用 <code>QtConcurrent</code>
      或独立线程处理，并通过信号回到主线程通知 UI。UI 层<strong>永远在主线程</strong>操作控件。
    </div>

    <h3>3.3 数据传递约定</h3>
    <ul>
      <li><strong>实体类共享</strong>：Entity 类定义在公共头文件中，前后端都使用同一套数据结构</li>
      <li><strong>只读访问</strong>：前端获取到 Entity 后只读，修改通过 Service 接口提交</li>
      <li><strong>智能指针</strong>：复杂对象使用 <code>QSharedPointer</code> 管理，避免深拷贝开销</li>
      <li><strong>分页结果</strong>：列表查询统一返回 <code>PageResult&lt;T&gt;</code>（含总数、当前页、每页条数、数据列表）</li>
      <li><strong>操作结果</strong>：写操作统一返回 <code>OperationResult</code>（含 success、message、data）</li>
    </ul>

    <pre><code>// 通用操作结果
struct OperationResult {
    bool success;
    QString message;       // 失败时的错误信息
    QVariant data;         // 附加返回数据（如新建的ID）
    static OperationResult ok(const QVariant& d = QVariant());
    static OperationResult fail(const QString& msg);
};

// 分页结果
template&lt;typename T&gt;
struct PageResult {
    QList&lt;T&gt; items;
    int totalCount;
    int page;
    int pageSize;
    int totalPages() const { return (totalCount + pageSize - 1) / pageSize; }
};</code></pre>
  </section>

  <footer>
    Qt 自动化测试配置软件 · 前后端功能详细设计 · 2026-09-05
  </footer>

</div>

<script src="./_shared/js/echarts.min.js"></script>
<script src="./_shared/js/mermaid.min.js"></script>

<script>
  function switchTab(tabId) {
    // 找到当前 section
    var section = document.getElementById(tabId).closest('section');
    var tabs = section.querySelectorAll('.tab-content');
    var buttons = section.querySelectorAll('.tab-btn');

    tabs.forEach(function(t) { t.classList.remove('active'); });
    buttons.forEach(function(b) { b.classList.remove('active'); });

    document.getElementById(tabId).classList.add('active');
    // 找到对应的按钮
    buttons.forEach(function(b) {
      if (b.textContent.trim() === getTabLabel(tabId) || b.getAttribute('onclick').includes(tabId)) {
        b.classList.add('active');
      }
    });
  }

  function getTabLabel(id) {
    var map = {
      'fe-main': '主框架',
      'fe-module-a': '数据块配置模块',
      'fe-module-b': '流程编排模块',
      'fe-module-c': '判读配置模块',
      'fe-common': '通用组件',
      'be-dal': '数据访问层',
      'be-module-a': '数据块服务',
      'be-module-b': '流程引擎服务',
      'be-module-c': '判读规则服务',
      'be-common': '公共服务'
    };
    return map[id] || '';
  }

  // Mermaid init
  if (window.mermaid) {
    mermaid.initialize({
      startOnLoad: true,
      theme: 'base',
      securityLevel: 'loose',
      themeVariables: {
        primaryColor: '#eff6ff',
        primaryTextColor: '#1a2332',
        primaryBorderColor: '#2563eb',
        lineColor: '#6b7a90',
        secondaryColor: '#ecfdf5',
        tertiaryColor: '#fef3c7',
        fontFamily: '-apple-system, "Noto Sans CJK SC", "Microsoft YaHei", sans-serif',
        fontSize: '13px'
      }
    });
  }
</script>
</body>
</html>
