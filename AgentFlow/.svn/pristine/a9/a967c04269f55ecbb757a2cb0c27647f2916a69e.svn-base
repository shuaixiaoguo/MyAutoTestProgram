<!-- Generated by Trae Work -->
<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Qt自动化测试配置软件 - 开发设计方案</title>
<style>
  :root {
    --bg: #f8f9fb;
    --bg2: #ffffff;
    --ink: #1a2332;
    --muted: #6b7a90;
    --rule: #e2e8f0;
    --accent: #2563eb;
    --accent2: #0891b2;
    --success: #059669;
    --warning: #d97706;
    --danger: #dc2626;
  }

  * { margin: 0; padding: 0; box-sizing: border-box; }

  body {
    font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", "Noto Sans CJK SC", "Microsoft YaHei", sans-serif;
    background: var(--bg);
    color: var(--ink);
    line-height: 1.7;
    font-size: 15px;
  }

  .container {
    max-width: 1080px;
    margin: 0 auto;
    padding: 2rem 1.5rem;
  }

  /* Header */
  .report-header {
    background: linear-gradient(135deg, #1e3a5f 0%, #2563eb 50%, #0891b2 100%);
    color: #fff;
    padding: 3rem 2rem;
    border-radius: 12px;
    margin-bottom: 2.5rem;
    position: relative;
    overflow: hidden;
  }
  .report-header::before {
    content: '';
    position: absolute;
    top: -50%;
    right: -10%;
    width: 400px;
    height: 400px;
    background: radial-gradient(circle, rgba(255,255,255,0.1) 0%, transparent 70%);
    border-radius: 50%;
  }
  .report-header h1 {
    font-size: 1.9rem;
    font-weight: 700;
    margin-bottom: 0.6rem;
    position: relative;
  }
  .report-header .subtitle {
    font-size: 1rem;
    opacity: 0.85;
    position: relative;
  }
  .report-header .meta {
    margin-top: 1.2rem;
    display: flex;
    gap: 1.5rem;
    font-size: 0.85rem;
    opacity: 0.75;
    position: relative;
    flex-wrap: wrap;
  }
  .report-header .meta span {
    display: flex;
    align-items: center;
    gap: 0.4rem;
  }

  /* Section */
  section {
    margin-bottom: 2.5rem;
  }
  h2 {
    font-size: 1.4rem;
    font-weight: 700;
    color: var(--ink);
    margin-bottom: 1rem;
    padding-bottom: 0.5rem;
    border-bottom: 2px solid var(--accent);
    display: flex;
    align-items: center;
    gap: 0.6rem;
  }
  h2 .num {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 28px;
    height: 28px;
    background: var(--accent);
    color: #fff;
    border-radius: 6px;
    font-size: 0.85rem;
    font-weight: 700;
  }
  h3 {
    font-size: 1.1rem;
    font-weight: 600;
    color: var(--ink);
    margin: 1.3rem 0 0.6rem;
  }
  h4 {
    font-size: 0.95rem;
    font-weight: 600;
    color: var(--accent);
    margin: 1rem 0 0.4rem;
  }
  p { margin-bottom: 0.7rem; }

  /* Cards */
  .card {
    background: var(--bg2);
    border: 1px solid var(--rule);
    border-radius: 8px;
    padding: 1.2rem;
    margin-bottom: 1rem;
  }
  .card-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
    gap: 1rem;
    margin: 1rem 0;
  }
  .card-grid .card {
    margin-bottom: 0;
  }
  .card h4 {
    margin-top: 0;
    color: var(--accent);
  }
  .card .icon {
    font-size: 1.5rem;
    margin-bottom: 0.5rem;
  }

  /* Tables */
  .table-wrap {
    overflow-x: auto;
    overflow-y: auto;
    max-height: 600px;
    margin: 1rem 0;
    border: 1px solid var(--rule);
    border-radius: 6px;
  }
  table {
    width: 100%;
    border-collapse: collapse;
    font-size: 0.9rem;
  }
  thead th {
    background: var(--accent);
    color: #fff;
    padding: 0.7rem 0.9rem;
    text-align: left;
    font-weight: 600;
    position: sticky;
    top: 0;
    z-index: 1;
  }
  tbody td {
    padding: 0.6rem 0.9rem;
    border-bottom: 1px solid var(--rule);
  }
  tbody tr:hover {
    background: #f1f5f9;
  }
  tbody tr:last-child td {
    border-bottom: none;
  }
  .code-col {
    font-family: "Consolas", "Monaco", monospace;
    font-size: 0.82rem;
    color: var(--accent);
  }

  /* Code */
  code {
    font-family: "Consolas", "Monaco", monospace;
    background: #f1f5f9;
    padding: 0.15rem 0.4rem;
    border-radius: 4px;
    font-size: 0.85em;
    color: var(--accent2);
  }
  pre {
    background: #1e293b;
    color: #e2e8f0;
    padding: 1rem 1.2rem;
    border-radius: 6px;
    overflow-x: auto;
    margin: 0.8rem 0;
    font-size: 0.85rem;
    line-height: 1.6;
  }
  pre code {
    background: none;
    padding: 0;
    color: inherit;
  }

  /* Diagram */
  .diagram {
    margin: 1.5rem 0;
    text-align: center;
  }
  .diagram svg,
  .diagram img {
    max-width: 100%;
    height: auto;
  }
  .diagram figcaption {
    font-size: 0.82rem;
    color: var(--muted);
    margin-top: 0.5rem;
    font-style: italic;
  }
  .mermaid {
    background: var(--bg2);
    border: 1px solid var(--rule);
    border-radius: 8px;
    padding: 1.2rem;
  }

  /* Chart */
  .chart-figure {
    margin: 1.5rem 0;
    background: var(--bg2);
    border: 1px solid var(--rule);
    border-radius: 8px;
    padding: 1.2rem;
  }
  .chart-figure figcaption {
    font-size: 0.9rem;
    font-weight: 600;
    color: var(--ink);
    margin-bottom: 0.8rem;
  }

  /* Callout */
  .callout {
    border-left: 4px solid var(--accent);
    background: #eff6ff;
    padding: 0.9rem 1.2rem;
    border-radius: 0 6px 6px 0;
    margin: 1rem 0;
    font-size: 0.92rem;
  }
  .callout.warning {
    border-color: var(--warning);
    background: #fffbeb;
  }
  .callout.success {
    border-color: var(--success);
    background: #ecfdf5;
  }
  .callout strong {
    color: var(--accent);
  }
  .callout.warning strong {
    color: var(--warning);
  }
  .callout.success strong {
    color: var(--success);
  }

  /* Team row */
  .team-lanes {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1rem;
    margin: 1rem 0;
  }
  .team-lane {
    background: var(--bg2);
    border: 1px solid var(--rule);
    border-radius: 8px;
    padding: 1rem;
    border-top: 3px solid var(--accent);
  }
  .team-lane.backend { border-top-color: var(--accent2); }
  .team-lane.ai { border-top-color: var(--success); }
  .team-lane h4 {
    margin-top: 0;
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }
  .team-lane ul {
    padding-left: 1.2rem;
    font-size: 0.88rem;
  }
  .team-lane li {
    margin-bottom: 0.35rem;
  }

  /* Phase timeline */
  .phase {
    display: flex;
    gap: 1rem;
    margin: 1rem 0;
    align-items: stretch;
  }
  .phase-label {
    flex: 0 0 100px;
    text-align: center;
    padding: 1rem 0.5rem;
    background: var(--accent);
    color: #fff;
    border-radius: 8px;
    font-weight: 600;
    font-size: 0.9rem;
    display: flex;
    flex-direction: column;
    justify-content: center;
  }
  .phase-label .week {
    font-size: 0.75rem;
    opacity: 0.8;
    margin-top: 0.3rem;
  }
  .phase-content {
    flex: 1;
    background: var(--bg2);
    border: 1px solid var(--rule);
    border-radius: 8px;
    padding: 0.9rem 1.1rem;
  }
  .phase-content ul {
    padding-left: 1.2rem;
    margin: 0;
  }
  .phase-content li {
    margin-bottom: 0.3rem;
    font-size: 0.9rem;
  }

  /* Tag / badge */
  .tag {
    display: inline-block;
    padding: 0.15rem 0.6rem;
    border-radius: 999px;
    font-size: 0.75rem;
    font-weight: 600;
    margin-right: 0.3rem;
  }
  .tag.frontend { background: #dbeafe; color: var(--accent); }
  .tag.backend { background: #cffafe; color: var(--accent2); }
  .tag.db { background: #dcfce7; color: var(--success); }
  .tag.ai { background: #fef3c7; color: var(--warning); }

  /* Footer */
  footer {
    margin-top: 3rem;
    padding-top: 1.5rem;
    border-top: 1px solid var(--rule);
    color: var(--muted);
    font-size: 0.85rem;
  }

  /* Responsive */
  @media (max-width: 768px) {
    .team-lanes { grid-template-columns: 1fr; }
    .phase { flex-direction: column; }
    .phase-label { flex: none; }
    .card-grid { grid-template-columns: 1fr; }
    h1 { font-size: 1.5rem; }
    h2 { font-size: 1.2rem; }
  }

  mark.key {
    background: none;
    color: var(--accent);
    font-weight: 600;
  }

  ul, ol {
    margin-left: 1.2rem;
    margin-bottom: 0.7rem;
  }
  li { margin-bottom: 0.3rem; }
</style>
</head>
<body>
<div class="container">

  <!-- Header -->
  <header class="report-header">
    <h1>Qt自动化测试配置软件 · 开发设计方案</h1>
    <div class="subtitle">基于 Qt 5.12.9 + MySQL 的可视化测试配置平台，支持数据块配置、流程编排与AI辅助</div>
    <div class="meta">
      <span>📅 2026-09-05</span>
      <span>🏗️ 版本 v1.0</span>
      <span>👥 支持3条线并行开发</span>
      <span>🔮 预留AI扩展能力</span>
    </div>
  </header>

  <!-- 1. 项目概述 -->
  <section>
    <h2><span class="num">1</span>项目概述</h2>

    <h3>1.1 项目目标</h3>
    <p>
      构建一款基于 <mark class="key">Qt 5.12.9</mark> 的桌面端自动化测试配置软件，提供可视化的参数配置、流程编排和判读规则定义能力，
      所有配置数据持久化存储于 <mark class="key">MySQL</mark> 数据库。系统架构预留 AI 接入层，为后续智能参数推荐、流程优化等能力扩展奠定基础。
    </p>

    <h3>1.2 核心功能域</h3>
    <div class="card-grid">
      <div class="card">
        <div class="icon">📊</div>
        <h4>参数配置模块</h4>
        <p style="font-size:0.88rem;color:var(--muted)">
          7级层级树形结构（总线类别→总线→设备→接口→路由→数据块→参数），
          支持节点增删改、属性面板编辑、级联删除、多选操作，数据持久化到 MySQL。
        </p>
      </div>
      <div class="card">
        <div class="icon">🔀</div>
        <h4>自动化流程编排</h4>
        <p style="font-size:0.88rem;color:var(--muted)">
          可视化拖拽编排测试指令序列，支持条件分支、循环、子流程嵌套，流程可保存、复用、版本追溯。
        </p>
      </div>
      <div class="card">
        <div class="icon">✅</div>
        <h4>判读参数配置</h4>
        <p style="font-size:0.88rem;color:var(--muted)">
          定义测试结果的判定规则，支持阈值比较、趋势判断、多条件组合逻辑，生成判读报告模板。
        </p>
      </div>
      <div class="card">
        <div class="icon">🤖</div>
        <h4>AI 辅助扩展</h4>
        <p style="font-size:0.88rem;color:var(--muted)">
          预留 AI 接入接口，支持参数推荐、流程生成、异常判读辅助等智能能力的后续接入。
        </p>
      </div>
    </div>

    <h3>1.3 设计原则</h3>
    <ul>
      <li><mark class="key">分层解耦</mark>：UI 层 / 业务逻辑层 / 数据访问层严格分离，各层通过接口通信，便于独立开发与测试</li>
      <li><mark class="key">数据先行</mark>：数据库 schema 优先确定并冻结，作为各模块开发的契约基础</li>
      <li><mark class="key">接口驱动</mark>：模块间通过明确的接口/信号槽交互，接口定义先行，实现可并行</li>
      <li><mark class="key">可扩展</mark>：AI 能力以插件化方式接入，不侵入核心业务逻辑</li>
    </ul>
  </section>

  <!-- 2. 系统架构 -->
  <section>
    <h2><span class="num">2</span>系统架构设计</h2>

    <h3>2.1 整体架构</h3>
    <p>系统采用经典的四层架构，各层职责清晰，依赖方向单向，支持多人并行开发。</p>

    <figure class="diagram">
      <pre class="mermaid">
graph TB
    subgraph UI["🖥️ UI 表现层 (Qt Widgets)"]
        direction LR
        U1[主窗口 / 导航框架]
        U2[数据块配置模块]
        U3[流程编排模块]
        U4[判读配置模块]
        U5[系统管理模块]
    end

    subgraph BLL["⚙️ 业务逻辑层 (Service)"]
        direction LR
        B1[数据块管理服务]
        B2[流程引擎服务]
        B3[判读规则服务]
        B4[配置版本服务]
    end

    subgraph DAL["💾 数据访问层 (DAO/Repository)"]
        direction LR
        D1[数据块DAO]
        D2[流程DAO]
        D3[判读DAO]
        D4[通用DAO基类]
    end

    subgraph DB["🗄️ 数据存储层 (MySQL 8.0)"]
        direction LR
        DB1[核心业务表]
        DB2[配置版本表]
        DB3[系统字典表]
    end

    subgraph AI["🤖 AI 扩展层 (Plugin)"]
        direction LR
        A1[参数推荐接口]
        A2[流程生成接口]
        A3[判读辅助接口]
    end

    UI --> BLL
    BLL --> DAL
    DAL --> DB
    BLL --> AI
    AI --> DAL
      </pre>
      <figcaption>图 2-1：系统四层架构图</figcaption>
    </figure>

    <h3>2.2 分层职责说明</h3>
    <div class="table-wrap">
      <table>
        <thead>
          <tr>
            <th>层级</th>
            <th>职责</th>
            <th>技术载体</th>
            <th>开发角色</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td><strong>UI 表现层</strong></td>
            <td>用户交互、界面渲染、表单校验、可视化编排</td>
            <td>Qt Widgets / Qt Designer / 自定义控件</td>
            <td>前端开发</td>
          </tr>
          <tr>
            <td><strong>业务逻辑层</strong></td>
            <td>业务规则、流程编排引擎、版本控制、AI调度</td>
            <td>C++ Service 类 / 信号槽 / 状态机</td>
            <td>后端开发</td>
          </tr>
          <tr>
            <td><strong>数据访问层</strong></td>
            <td>ORM 封装、SQL 执行、事务管理、连接池</td>
            <td>Qt SQL / QSqlDatabase / 自定义 DAO</td>
            <td>后端开发</td>
          </tr>
          <tr>
            <td><strong>数据存储层</strong></td>
            <td>数据持久化、索引、约束、存储过程</td>
            <td>MySQL 8.0 / InnoDB</td>
            <td>DBA / 后端</td>
          </tr>
          <tr>
            <td><strong>AI 扩展层</strong></td>
            <td>AI 能力封装、模型调用、结果适配</td>
            <td>Qt Plugin 框架 / HTTP 客户端</td>
            <td>AI 开发</td>
          </tr>
        </tbody>
      </table>
    </div>

    <h3>2.3 模块划分与并行边界</h3>
    <p>三大业务模块可独立并行开发，模块间通过数据库表 + 服务接口形成契约。</p>

    <figure class="diagram">
      <pre class="mermaid">
graph LR
    subgraph ModuleA["模块 A：参数配置"]
        A1[总线类别管理]
        A2[总线/设备/接口管理]
        A3[路由/数据块管理]
        A4[参数位定义]
        A5[属性面板编辑]
    end

    subgraph ModuleB["模块 B：自动化流程编排"]
        B1[指令库]
        B2[流程画布]
        B3[流程引擎]
        B4[调试执行]
    end

    subgraph ModuleC["模块 C：判读参数配置"]
        C1[判读规则]
        C2[阈值配置]
        C3[逻辑组合]
        C4[报告模板]
    end

    DB_MYSQL[(MySQL 数据库)]

    ModuleA -->|读写| DB_MYSQL
    ModuleB -->|读写| DB_MYSQL
    ModuleC -->|读写| DB_MYSQL

    ModuleB -->|引用数据块| ModuleA
    ModuleC -->|引用流程+数据块| ModuleB
    ModuleC -->|引用数据块| ModuleA
      </pre>
      <figcaption>图 2-2：三大业务模块依赖关系</figcaption>
    </figure>

    <div class="callout success">
      <strong>并行开发策略：</strong>
      数据块模块（A）优先级最高，流程模块（B）和判读模块（C）依赖 A 的数据结构，
      但可先基于约定的接口/mock 数据并行开发，待 A 完成后接入真实数据。
    </div>
  </section>

  <!-- 3. 数据库设计 -->
  <section>
    <h2><span class="num">3</span>数据库设计</h2>

    <p>
      数据库是各模块开发的契约基础，<mark class="key">必须在开发启动前完成 schema 设计与评审</mark>，
      一旦进入开发阶段，表结构变更需走严格的变更流程。
    </p>

    <h3>3.1 核心表概览</h3>

    <h4>3.1.1 参数配置模块（7 张表，7 级层级）</h4>
    <p>参数配置采用 7 级树形层级结构：<strong>总线类别 → 总线 → 设备 → 接口 → 路由 → 数据块 → 参数</strong>，每张表对应一级节点，通过外键逐级关联。</p>
    <div class="table-wrap">
      <table>
        <thead>
          <tr>
            <th>层级</th>
            <th>表名</th>
            <th>中文名称</th>
            <th>父级外键</th>
            <th>说明</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td>1</td>
            <td class="code-col">bus_category</td>
            <td>总线类别表</td>
            <td>—（根级）</td>
            <td>总线的分类管理：名称、描述、排序</td>
          </tr>
          <tr>
            <td>2</td>
            <td class="code-col">bus</td>
            <td>总线表</td>
            <td>category_id</td>
            <td>总线定义：名称、编码、总线类型、协议版本、最大数据块长度</td>
          </tr>
          <tr>
            <td>3</td>
            <td class="code-col">bus_device</td>
            <td>设备表</td>
            <td>bus_id</td>
            <td>总线下的设备：名称、编码、设备类型、描述</td>
          </tr>
          <tr>
            <td>4</td>
            <td class="code-col">bus_interface</td>
            <td>接口表</td>
            <td>device_id</td>
            <td>设备通信接口：名称、适配器、配置、宿主IP、硬件地址、是否自动启动</td>
          </tr>
          <tr>
            <td>5</td>
            <td class="code-col">bus_route</td>
            <td>路由表</td>
            <td>interface_id</td>
            <td>数据路由：名称、方向（发送/接收/双向）、处理方式（周期/事件/DMA/中断）、周期(ms)</td>
          </tr>
          <tr>
            <td>6</td>
            <td class="code-col">bus_datablock</td>
            <td>数据块表</td>
            <td>route_id</td>
            <td>数据块定义：块名称、是否指定块长度、长度(字节)、触发模式、描述</td>
          </tr>
          <tr>
            <td>7</td>
            <td class="code-col">bus_parameter</td>
            <td>参数表</td>
            <td>datablock_id</td>
            <td>参数详细定义：名称、类型、起始位、位宽、字节序、精度、计算公式、枚举定义</td>
          </tr>
        </tbody>
      </table>
    </div>

    <div class="callout">
      <strong>设计要点：</strong>
      <ul>
        <li>每张表均有完整的 CRUD 操作，同类型节点名称不可重复（<code>nameExistsForType</code> 检查）</li>
        <li>级联删除：删除父节点时自动删除所有子节点及其后代</li>
        <li><code>precision</code> 是 MySQL 保留字，SQL 中使用反引号转义</li>
        <li>所有表均包含 <code>created_at</code> / <code>updated_at</code> 时间戳字段</li>
      </ul>
    </div>

    <h4>3.1.2 其他模块表（规划中）</h4>
    <div class="table-wrap">
      <table>
        <thead>
          <tr>
            <th>表名</th>
            <th>中文名称</th>
            <th>所属模块</th>
            <th>说明</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td class="code-col">t_instruction</td>
            <td>指令定义表</td>
            <td><span class="tag backend">B</span></td>
            <td>原子指令库：指令类型、参数格式、执行器标识</td>
          </tr>
          <tr>
            <td class="code-col">t_flow</td>
            <td>流程定义表</td>
            <td><span class="tag backend">B</span></td>
            <td>自动化流程：名称、描述、版本、状态</td>
          </tr>
          <tr>
            <td class="code-col">t_flow_node</td>
            <td>流程节点表</td>
            <td><span class="tag backend">B</span></td>
            <td>流程中的节点：节点类型、关联指令、参数配置、位置信息</td>
          </tr>
          <tr>
            <td class="code-col">t_flow_edge</td>
            <td>流程连线表</td>
            <td><span class="tag backend">B</span></td>
            <td>节点间的连接关系：起点、终点、条件表达式</td>
          </tr>
          <tr>
            <td class="code-col">t_judge_rule</td>
            <td>判读规则表</td>
            <td><span class="tag db">C</span></td>
            <td>判读规则定义：名称、关联数据块、优先级</td>
          </tr>
          <tr>
            <td class="code-col">t_judge_condition</td>
            <td>判读条件表</td>
            <td><span class="tag db">C</span></td>
            <td>具体判定条件：字段、比较符、阈值、逻辑运算符</td>
          </tr>
          <tr>
            <td class="code-col">t_config_version</td>
            <td>配置版本表</td>
            <td><span class="tag db">公共</span></td>
            <td>所有配置的版本快照：类型、关联ID、版本号、JSON快照</td>
          </tr>
          <tr>
            <td class="code-col">t_sys_dict</td>
            <td>系统字典表</td>
            <td><span class="tag db">公共</span></td>
            <td>枚举值、下拉选项等系统级字典数据</td>
          </tr>
          <tr>
            <td class="code-col">t_ai_suggestion</td>
            <td>AI 建议记录表</td>
            <td><span class="tag ai">AI</span></td>
            <td>记录 AI 给出的参数/流程建议及用户采纳情况</td>
          </tr>
        </tbody>
      </table>
    </div>

    <h3>3.2 ER 关系示意</h3>

    <h4>3.2.1 参数配置 7 级层级关系</h4>
    <figure class="diagram">
      <pre class="mermaid">
erDiagram
    bus_category ||--o{ bus : "包含"
    bus ||--o{ bus_device : "包含"
    bus_device ||--o{ bus_interface : "包含"
    bus_interface ||--o{ bus_route : "包含"
    bus_route ||--o{ bus_datablock : "包含"
    bus_datablock ||--o{ bus_parameter : "包含"

    bus_category {
        int id PK
        varchar name
        text description
        int sort_order
        datetime created_at
        datetime updated_at
    }
    bus {
        int id PK
        int category_id FK
        varchar name
        varchar code
        varchar bus_type
        varchar protocol_version
        text description
        datetime created_at
        datetime updated_at
    }
    bus_device {
        int id PK
        int bus_id FK
        varchar name
        varchar code
        varchar device_type
        text description
        datetime created_at
        datetime updated_at
    }
    bus_interface {
        int id PK
        int device_id FK
        varchar name
        varchar code
        varchar if_type
        varchar adapter_name
        varchar adapter_ip
        int port
        int baudrate
        varchar device_address
        text description
        datetime created_at
        datetime updated_at
    }
    bus_route {
        int id PK
        int interface_id FK
        varchar name
        int direction
        int process_mode
        int period_ms
        text description
        datetime created_at
        datetime updated_at
    }
    bus_datablock {
        int id PK
        int route_id FK
        varchar name
        varchar code
        int data_length
        varchar trigger_mode
        text description
        int sort_order
        datetime created_at
        datetime updated_at
    }
    bus_parameter {
        int id PK
        int datablock_id FK
        varchar name
        varchar code
        varchar param_type
        int start_bit
        int bit_width
        int byte_order
        varchar unit
        int precision
        varchar default_value
        varchar min_value
        varchar max_value
        varchar formula
        text enum_options
        text description
        int sort_order
        datetime created_at
        datetime updated_at
    }
      </pre>
      <figcaption>图 3-1：参数配置 7 级层级实体关系图</figcaption>
    </figure>

    <h4>3.2.2 跨模块关系（规划中）</h4>
    <figure class="diagram">
      <pre class="mermaid">
erDiagram
    t_flow ||--o{ t_flow_node : "包含"
    t_flow ||--o{ t_flow_edge : "包含"
    t_flow_node }o--|| t_instruction : "引用"
    t_flow_node }o--o| bus_datablock : "绑定数据块"
    t_judge_rule ||--o{ t_judge_condition : "包含"
    t_judge_rule }o--|| bus_datablock : "关联数据块"
    t_judge_rule }o--o| t_flow : "关联流程"
    t_config_version }o--o| bus_datablock : "版本化"
    t_config_version }o--o| t_flow : "版本化"
    t_config_version }o--o| t_judge_rule : "版本化"
      </pre>
      <figcaption>图 3-2：跨模块实体关系图（流程、判读模块）</figcaption>
    </figure>

    <h3>3.3 建表 SQL 示例（参数配置模块）</h3>
    <pre><code>-- 1. 总线类别表
CREATE TABLE bus_category (
    id            INT AUTO_INCREMENT PRIMARY KEY,
    name          VARCHAR(64)  NOT NULL COMMENT '类别名称',
    description   TEXT         DEFAULT NULL COMMENT '描述',
    sort_order    INT          DEFAULT 0 COMMENT '排序',
    created_at    DATETIME     DEFAULT NULL,
    updated_at    DATETIME     DEFAULT NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='总线类别表';

-- 2. 总线表
CREATE TABLE bus (
    id              INT AUTO_INCREMENT PRIMARY KEY,
    category_id     INT          NOT NULL COMMENT '所属类别ID',
    name            VARCHAR(64)  NOT NULL COMMENT '总线名称',
    code            VARCHAR(64)  NOT NULL COMMENT '总线编码',
    bus_type        VARCHAR(32)  DEFAULT NULL COMMENT '总线类型',
    protocol_version VARCHAR(32) DEFAULT NULL COMMENT '协议版本',
    description     TEXT         DEFAULT NULL COMMENT '描述',
    created_at      DATETIME     DEFAULT NULL,
    updated_at      DATETIME     DEFAULT NULL,
    INDEX idx_category (category_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='总线表';

-- 3. 设备表
CREATE TABLE bus_device (
    id            INT AUTO_INCREMENT PRIMARY KEY,
    bus_id        INT          NOT NULL COMMENT '所属总线ID',
    name          VARCHAR(64)  NOT NULL COMMENT '设备名称',
    code          VARCHAR(64)  NOT NULL COMMENT '设备编码',
    device_type   VARCHAR(32)  DEFAULT NULL COMMENT '设备类型',
    description   TEXT         DEFAULT NULL COMMENT '描述',
    created_at    DATETIME     DEFAULT NULL,
    updated_at    DATETIME     DEFAULT NULL,
    INDEX idx_bus (bus_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='设备表';

-- 4. 接口表
CREATE TABLE bus_interface (
    id              INT AUTO_INCREMENT PRIMARY KEY,
    device_id       INT          NOT NULL COMMENT '所属设备ID',
    name            VARCHAR(64)  NOT NULL COMMENT '接口名称',
    code            VARCHAR(64)  NOT NULL COMMENT '接口编码',
    if_type         VARCHAR(32)  DEFAULT NULL COMMENT '接口类型',
    adapter_name    VARCHAR(64)  DEFAULT NULL COMMENT '适配器名称',
    adapter_ip      VARCHAR(64)  DEFAULT NULL COMMENT '适配器IP',
    port            INT          DEFAULT 0 COMMENT '端口',
    baudrate        INT          DEFAULT 0 COMMENT '波特率',
    device_address  VARCHAR(64)  DEFAULT NULL COMMENT '设备地址',
    description     TEXT         DEFAULT NULL COMMENT '描述',
    created_at      DATETIME     DEFAULT NULL,
    updated_at      DATETIME     DEFAULT NULL,
    INDEX idx_device (device_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='接口表';

-- 5. 路由表
CREATE TABLE bus_route (
    id            INT AUTO_INCREMENT PRIMARY KEY,
    interface_id  INT          NOT NULL COMMENT '所属接口ID',
    name          VARCHAR(64)  NOT NULL COMMENT '路由名称',
    direction     INT          DEFAULT 1 COMMENT '方向:1发送 2接收 3双向',
    process_mode  INT          DEFAULT 1 COMMENT '处理方式:1周期 2事件 3DMA 4中断',
    period_ms     INT          DEFAULT 100 COMMENT '周期(ms)',
    description   TEXT         DEFAULT NULL COMMENT '描述',
    created_at    DATETIME     DEFAULT NULL,
    updated_at    DATETIME     DEFAULT NULL,
    INDEX idx_interface (interface_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='路由表';

-- 6. 数据块表
CREATE TABLE bus_datablock (
    id            INT AUTO_INCREMENT PRIMARY KEY,
    route_id      INT          NOT NULL COMMENT '所属路由ID',
    name          VARCHAR(64)  NOT NULL COMMENT '数据块名称',
    code          VARCHAR(64)  NOT NULL COMMENT '数据块编码',
    data_length   INT          DEFAULT 0 COMMENT '数据长度(字节)',
    trigger_mode  VARCHAR(32)  DEFAULT NULL COMMENT '触发模式',
    description   TEXT         DEFAULT NULL COMMENT '描述',
    sort_order    INT          DEFAULT 0 COMMENT '排序',
    created_at    DATETIME     DEFAULT NULL,
    updated_at    DATETIME     DEFAULT NULL,
    INDEX idx_route (route_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='数据块表';

-- 7. 参数表
CREATE TABLE bus_parameter (
    id              INT AUTO_INCREMENT PRIMARY KEY,
    datablock_id    INT          NOT NULL COMMENT '所属数据块ID',
    name            VARCHAR(64)  NOT NULL COMMENT '参数名称',
    code            VARCHAR(64)  NOT NULL COMMENT '参数编码',
    param_type      VARCHAR(32)  DEFAULT NULL COMMENT '参数类型:uint8/uint16/.../float/double/bool/enum',
    start_bit       INT          DEFAULT 0 COMMENT '起始位',
    bit_width       INT          DEFAULT 8 COMMENT '位宽',
    byte_order      INT          DEFAULT 1 COMMENT '字节序:1大端 2小端',
    unit            VARCHAR(32)  DEFAULT NULL COMMENT '单位',
    `precision`     INT          DEFAULT 0 COMMENT '精度(precision为MySQL保留字,用反引号)',
    default_value   VARCHAR(128) DEFAULT NULL COMMENT '默认值',
    min_value       VARCHAR(128) DEFAULT NULL COMMENT '最小值',
    max_value       VARCHAR(128) DEFAULT NULL COMMENT '最大值',
    formula         VARCHAR(256) DEFAULT NULL COMMENT '计算公式',
    enum_options    TEXT         DEFAULT NULL COMMENT '枚举选项JSON数组',
    description     TEXT         DEFAULT NULL COMMENT '描述',
    sort_order      INT          DEFAULT 0 COMMENT '排序',
    created_at      DATETIME     DEFAULT NULL,
    updated_at      DATETIME     DEFAULT NULL,
    INDEX idx_datablock (datablock_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='参数表';</code></pre>

    <div class="callout warning">
      <strong>注意：</strong>
      <code>precision</code> 是 MySQL 保留字，在 SQL 语句中必须使用反引号 <code>`precision`</code> 转义，
      否则会导致建表或查询失败。
    </div>
  </section>

  <!-- 4. 技术栈 -->
  <section>
    <h2><span class="num">4</span>技术栈选型</h2>

    <h3>4.1 前端技术栈（Qt 5.12.9）</h3>
    <div class="card-grid">
      <div class="card">
        <h4>UI 框架</h4>
        <p style="font-size:0.88rem"><code>Qt Widgets</code> — 传统桌面控件，稳定成熟，适合企业级工具软件</p>
      </div>
      <div class="card">
        <h4>模型视图</h4>
        <p style="font-size:0.88rem"><code>QTreeView + QStandardItemModel</code> — 7级树形结构，数据与视图分离，支持多选和级联操作</p>
      </div>
      <div class="card">
        <h4>流程画布</h4>
        <p style="font-size:0.88rem"><code>Graphics View Framework</code> — QGraphicsScene/View 实现拖拽式流程编排</p>
      </div>
      <div class="card">
        <h4>属性面板</h4>
        <p style="font-size:0.88rem"><code>NodePropertyPanel + ParamPropertyPanel</code> — 非参数节点用 QTableWidget 两列表单，参数节点用详细表单（基本信息/位信息/数值属性/计算公式/枚举定义）</p>
      </div>
      <div class="card">
        <h4>样式美化</h4>
        <p style="font-size:0.88rem"><code>QSS (Qt Style Sheet)</code> — 类 CSS 语法，统一界面风格</p>
      </div>
      <div class="card">
        <h4>国际化</h4>
        <p style="font-size:0.88rem"><code>Qt Linguist / tr()</code> — 预留中英文切换能力</p>
      </div>
    </div>

    <h3>4.2 后端技术栈</h3>
    <div class="card-grid">
      <div class="card">
        <h4>数据库访问</h4>
        <p style="font-size:0.88rem"><code>Qt SQL (QSqlDatabase)</code> + MySQL 驱动，封装 DAO 层</p>
      </div>
      <div class="card">
        <h4>JSON 处理</h4>
        <p style="font-size:0.88rem"><code>QJsonDocument / QJsonObject</code> — 配置快照、API 交互</p>
      </div>
      <div class="card">
        <h4>流程引擎</h4>
        <p style="font-size:0.88rem"><code>QStateMachine</code> + 自定义节点执行器，支持异步执行</p>
      </div>
      <div class="card">
        <h4>插件框架</h4>
        <p style="font-size:0.88rem"><code>Qt Plugin System</code> — AI 能力以插件形式动态加载</p>
      </div>
      <div class="card">
        <h4>HTTP 客户端</h4>
        <p style="font-size:0.88rem"><code>QNetworkAccessManager</code> — 调用 AI 服务 REST API</p>
      </div>
      <div class="card">
        <h4>日志系统</h4>
        <p style="font-size:0.88rem"><code>qInstallMessageHandler</code> + 文件输出，支持分级日志</p>
      </div>
    </div>

    <h3>4.3 数据库</h3>
    <ul>
      <li><strong>MySQL 8.0</strong> — 使用 InnoDB 引擎，utf8mb4 字符集</li>
      <li><strong>连接池</strong>：自实现基于 <code>QSqlDatabase</code> 的连接池，管理多连接复用</li>
      <li><strong>迁移工具</strong>：预留数据库版本升级机制（SQL 脚本 + 版本号表）</li>
    </ul>
  </section>

  <!-- 5. 并行开发组织 -->
  <section>
    <h2><span class="num">5</span>并行开发组织</h2>

    <p>
      按三大业务模块 + 公共基础组建三条开发线，<mark class="key">数据库 schema 和公共基础库为最高优先级</mark>，
      在开发第一周完成并冻结，随后三条线全速并行。
    </p>

    <h3>5.1 三条开发线分工</h3>
    <div class="team-lanes">
      <div class="team-lane frontend">
        <h4>🎨 前端开发线</h4>
        <p style="font-size:0.82rem;color:var(--muted);margin-bottom:0.5rem">负责 UI 表现层</p>
        <ul>
          <li>主窗口框架与导航</li>
          <li>数据块配置界面（模块 A）</li>
          <li>流程编排画布（模块 B-UI）</li>
          <li>判读配置界面（模块 C-UI）</li>
          <li>通用控件与 QSS 样式</li>
          <li>AI 辅助交互界面</li>
        </ul>
      </div>
      <div class="team-lane backend">
        <h4>⚙️ 后端开发线</h4>
        <p style="font-size:0.82rem;color:var(--muted);margin-bottom:0.5rem">负责业务逻辑+数据访问</p>
        <ul>
          <li>数据库 schema 设计</li>
          <li>DAO 层与连接池</li>
          <li>数据块管理服务（模块 A）</li>
          <li>流程引擎与执行器（模块 B）</li>
          <li>判读规则计算服务（模块 C）</li>
          <li>版本管理服务</li>
        </ul>
      </div>
      <div class="team-lane ai">
        <h4>🤖 AI 扩展线</h4>
        <p style="font-size:0.82rem;color:var(--muted);margin-bottom:0.5rem">负责AI能力接入</p>
        <ul>
          <li>AI 插件接口定义</li>
          <li>参数推荐服务封装</li>
          <li>流程生成服务封装</li>
          <li>判读辅助服务封装</li>
          <li>AI 服务 HTTP 客户端</li>
          <li>建议记录与反馈收集</li>
        </ul>
      </div>
    </div>

    <h3>5.2 模块间接口契约</h3>
    <p>各模块通过 Service 类的公共接口交互，接口先行评审，实现可并行。以下为核心接口示例：</p>

    <h4>数据块服务接口（模块 A 对外提供）</h4>
    <pre><code>// IDataBlockService.h — 模块A对外接口契约
class IDataBlockService {
public:
    virtual ~IDataBlockService() = default;

    // 获取数据块列表
    virtual QList&lt;DataBlockPtr&gt; listBlocks(
        const QString&amp; type = QString()) = 0;

    // 根据ID获取数据块（含字段）
    virtual DataBlockPtr getBlock(qint64 blockId) = 0;

    // 创建新版本数据块
    virtual qint64 createBlock(const DataBlock&amp; block) = 0;

    // 更新数据块（自动升版本）
    virtual bool updateBlock(const DataBlock&amp; block) = 0;

    // 获取字段列表
    virtual QList&lt;DataFieldPtr&gt; listFields(qint64 blockId) = 0;
};</code></pre>

    <h4>流程引擎服务接口（模块 B 对外提供）</h4>
    <pre><code>// IFlowEngineService.h — 模块B对外接口契约
class IFlowEngineService {
public:
    virtual ~IFlowEngineService() = default;

    // 保存/更新流程定义
    virtual qint64 saveFlow(const FlowDefinition&amp; flow) = 0;

    // 加载流程定义
    virtual FlowDefinitionPtr loadFlow(qint64 flowId) = 0;

    // 校验流程完整性
    virtual bool validateFlow(qint64 flowId,
                              QStringList&amp; errors) = 0;

    // 执行流程（异步，通过信号通知状态）
    virtual void executeFlow(qint64 flowId,
                             const QVariantMap&amp; context) = 0;

signals:
    void flowStarted(qint64 flowId);
    void nodeExecuted(qint64 nodeId, bool success);
    void flowFinished(qint64 flowId, bool success);
};</code></pre>

    <h4>AI 服务接口（AI 扩展层对外提供）</h4>
    <pre><code>// IAiAssistant.h — AI扩展层接口契约
class IAiAssistant : public QObject {
    Q_OBJECT
public:
    virtual ~IAiAssistant() = default;
    virtual QString name() const = 0;

    // 参数推荐：基于数据块ID给出字段建议值
    virtual Q_INVOKABLE QVariantMap suggestParams(
        qint64 blockId,
        const QVariantMap&amp; currentConfig) = 0;

    // 流程生成：根据自然语言描述生成流程骨架
    virtual Q_INVOKABLE FlowDefinitionPtr generateFlow(
        const QString&amp; description,
        const QStringList&amp; availableInstructions) = 0;

    // 判读辅助：分析异常数据，给出判读建议
    virtual Q_INVOKABLE QString assistJudgement(
        qint64 ruleId,
        const QVariantMap&amp; testData) = 0;

signals:
    void suggestionReady(const QString&amp; type, const QVariant&amp; data);
    void errorOccurred(const QString&amp; message);
};</code></pre>

    <div class="callout warning">
      <strong>接口评审机制：</strong>
      每个 Service 接口完成初稿后，必须组织上下游开发人员共同评审，确认接口签名、参数语义、
      异常处理方式达成一致后，方可进入实现阶段。接口变更需同步通知所有依赖方。
    </div>
  </section>

  <!-- 6. 实施路线图 -->
  <section>
    <h2><span class="num">6</span>实施路线图</h2>

    <p>分四个阶段推进，每个阶段结束有明确的交付物和评审节点。预计总周期约 <mark class="key">14 周</mark>，
      核心功能（阶段 1-3）可在 10 周内完成并交付可用版本。</p>

    <h3>6.1 阶段一：基础搭建（第 1-2 周）</h3>
    <div class="phase">
      <div class="phase-label">
        阶段一
        <span class="week">第 1-2 周</span>
      </div>
      <div class="phase-content">
        <ul>
          <li><strong>数据库 schema</strong>：完成所有核心表设计、建表 SQL、初始字典数据</li>
          <li><strong>项目框架搭建</strong>：Qt 工程结构、分层目录、.pro 文件配置</li>
          <li><strong>公共基础库</strong>：日志系统、数据库连接池、DAO 基类、通用工具类</li>
          <li><strong>主窗口框架</strong>：主窗口、菜单栏、状态栏、导航栏、多文档区域</li>
          <li><strong>接口契约评审</strong>：三大模块 Service 接口签名评审通过</li>
          <li><strong>AI 插件框架</strong>：Qt Plugin 骨架、接口定义、加载器实现</li>
        </ul>
        <p style="margin-top:0.5rem;font-size:0.85rem;color:var(--muted)">
          <strong>交付物：</strong>可运行的空壳程序 + 数据库初始化脚本 + 接口文档
        </p>
      </div>
    </div>

    <h3>6.2 阶段二：核心模块开发（第 3-7 周）</h3>
    <div class="phase">
      <div class="phase-label" style="background:var(--accent2)">
        阶段二
        <span class="week">第 3-7 周</span>
      </div>
      <div class="phase-content">
        <p style="margin-bottom:0.4rem"><strong>三条线并行推进：</strong></p>
        <ul>
          <li><strong>数据块模块（A）</strong> — 数据块 CRUD、字段编辑、参数模板、版本管理（优先级：高）</li>
          <li><strong>流程编排模块（B）</strong> — 指令库管理、流程画布、节点连线、属性面板</li>
          <li><strong>判读配置模块（C）</strong> — 规则配置、条件编辑、逻辑组合、阈值设置</li>
        </ul>
        <p style="margin-top:0.5rem;font-size:0.85rem;color:var(--muted)">
          <strong>关键里程碑：</strong>第 4 周末数据块模块完成并交付 → 流程和判读模块可接入真实数据
        </p>
      </div>
    </div>

    <h3>6.3 阶段三：集成与流程引擎（第 8-10 周）</h3>
    <div class="phase">
      <div class="phase-label" style="background:var(--success)">
        阶段三
        <span class="week">第 8-10 周</span>
      </div>
      <div class="phase-content">
        <ul>
          <li><strong>流程引擎实现</strong>：节点执行器、条件分支、循环控制、异常处理</li>
          <li><strong>判读计算引擎</strong>：条件求值、多规则组合运算、判读结果生成</li>
          <li><strong>模块集成联调</strong>：三大模块端到端打通，数据流转验证</li>
          <li><strong>导入导出</strong>：配置数据的 JSON/XML 导入导出功能</li>
          <li><strong>Bug 修复与体验优化</strong>：第一轮集成测试与问题修复</li>
        </ul>
        <p style="margin-top:0.5rem;font-size:0.85rem;color:var(--muted)">
          <strong>交付物：</strong>可用的 Beta 版本，完整支持配置 → 编排 → 判读全链路
        </p>
      </div>
    </div>

    <h3>6.4 阶段四：AI 接入与完善（第 11-14 周）</h3>
    <div class="phase">
      <div class="phase-label" style="background:var(--warning)">
        阶段四
        <span class="week">第 11-14 周</span>
      </div>
      <div class="phase-content">
        <ul>
          <li><strong>AI 参数推荐</strong>：接入 AI 服务，在数据块配置界面提供建议值</li>
          <li><strong>AI 流程生成</strong>：自然语言描述 → 自动生成流程骨架</li>
          <li><strong>AI 判读辅助</strong>：异常数据智能分析，辅助人工判读</li>
          <li><strong>建议反馈机制</strong>：用户采纳/拒绝 AI 建议的数据收集闭环</li>
          <li><strong>性能优化</strong>：大数据量场景下的界面响应、数据库查询优化</li>
          <li><strong>正式发布</strong>：安装包制作、用户手册、上线部署</li>
        </ul>
        <p style="margin-top:0.5rem;font-size:0.85rem;color:var(--muted)">
          <strong>交付物：</strong>v1.0 正式版本，含 AI 辅助能力
        </p>
      </div>
    </div>

    <h3>6.5 开发进度甘特图</h3>
    <figure class="chart-figure">
      <figcaption>图 6-1：各开发线并行进度安排</figcaption>
      <div id="chart-gantt" style="width:100%;min-height:480px"></div>
    </figure>
  </section>

  <!-- 7. AI 扩展设计 -->
  <section>
    <h2><span class="num">7</span>AI 扩展设计</h2>

    <p>
      AI 能力采用 <mark class="key">插件化 + 服务端调用</mark> 的架构，Qt 端仅做界面交互和结果展示，
      核心 AI 推理部署在后端服务，通过 REST API 调用。这种设计的好处是 Qt 端不依赖重型 AI 库，
      模型升级不影响客户端。
    </p>

    <h3>7.1 AI 接入架构</h3>
    <figure class="diagram">
      <pre class="mermaid">
graph TB
    subgraph QtClient["Qt 客户端"]
        UI_A[参数配置界面]
        UI_B[流程编排界面]
        UI_C[判读配置界面]
        PluginMgr[AI 插件管理器]
    end

    subgraph AIPlugin["AI 插件层 (Qt Plugin)"]
        P1[参数推荐插件]
        P2[流程生成插件]
        P3[判读辅助插件]
    end

    subgraph AIService["AI 服务端 (Python/Go)"]
        S1[参数推荐模型]
        S2[流程生成模型]
        S3[判读分析模型]
        S4[知识库/RAG]
    end

    subgraph Feedback["反馈闭环"]
        F1[建议记录表]
        F2[效果分析]
        F3[模型微调]
    end

    UI_A -->|调用| PluginMgr
    UI_B -->|调用| PluginMgr
    UI_C -->|调用| PluginMgr
    PluginMgr -->|加载| P1
    PluginMgr -->|加载| P2
    PluginMgr -->|加载| P3
    P1 -->|HTTP| S1
    P2 -->|HTTP| S2
    P3 -->|HTTP| S3
    S1 --> S4
    S2 --> S4
    S3 --> S4
    UI_A -->|采纳/拒绝| F1
    F1 --> F2 --> F3 --> S1
      </pre>
      <figcaption>图 7-1：AI 扩展架构与反馈闭环</figcaption>
    </figure>

    <h3>7.2 插件接口设计</h3>
    <p>AI 插件继承自 <code>IAiAssistant</code> 接口（见 5.2 节），使用 Qt 的 <code>Q_PLUGIN_METADATA</code> 宏声明。插件管理器在程序启动时扫描插件目录，动态加载可用的 AI 插件。</p>

    <pre><code>// 插件元数据 JSON 示例
{
    "name": "DefaultAiAssistant",
    "version": "1.0.0",
    "vendor": "YourCompany",
    "description": "默认 AI 助手插件",
    "capabilities": ["param_suggest", "flow_generate", "judge_assist"],
    "api_endpoint": "http://ai-service:8080/api/v1"
}</code></pre>

    <h3>7.3 阶段接入策略</h3>
    <ul>
      <li><strong>第一阶段（建议式）</strong>：AI 仅提供建议，用户手动采纳，不直接修改配置 — 风险最低</li>
      <li><strong>第二阶段（半自动）</strong>：支持一键应用 AI 建议，提供撤销回退能力</li>
      <li><strong>第三阶段（全自动）</strong>：在白名单场景下，AI 可自动生成完整配置并提交审核</li>
    </ul>
  </section>

  <!-- 8. 风险与应对 -->
  <section>
    <h2><span class="num">8</span>风险与应对</h2>

    <div class="table-wrap">
      <table>
        <thead>
          <tr>
            <th>风险项</th>
            <th>影响</th>
            <th>概率</th>
            <th>应对措施</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td>Qt Graphics View 流程画布性能问题</td>
            <td>节点多时卡顿</td>
            <td>中</td>
            <td>提前做性能原型验证；超过 200 节点时考虑分层渲染或 WebEngine 嵌入方案</td>
          </tr>
          <tr>
            <td>数据库设计变更波及面广</td>
            <td>多模块返工</td>
            <td>中</td>
            <td>阶段一集中评审 schema；预留扩展字段；使用迁移脚本管理版本</td>
          </tr>
          <tr>
            <td>Qt 5.12.9 兼容性限制</td>
            <td>部分新特性不可用</td>
            <td>高</td>
            <td>明确禁止使用 Qt 5.13+ API；代码评审时重点检查</td>
          </tr>
          <tr>
            <td>AI 服务不可用</td>
            <td>AI 功能失效</td>
            <td>中</td>
            <td>AI 功能完全降级可选，不影响核心配置能力；超时/失败有友好提示</td>
          </tr>
          <tr>
            <td>并行开发接口联调困难</td>
            <td>集成阶段延期</td>
            <td>中</td>
            <td>接口先行 + Mock 实现 + 每周固定联调时间</td>
          </tr>
          <tr>
            <td>MySQL 驱动部署问题</td>
            <td>程序无法连接数据库</td>
            <td>低</td>
            <td>提前编译好 Qt MySQL 驱动插件；安装包内置依赖库</td>
          </tr>
        </tbody>
      </table>
    </div>
  </section>

  <!-- 9. 下一步行动 -->
  <section>
    <h2><span class="num">9</span>下一步行动</h2>

    <div class="card-grid">
      <div class="card">
        <div class="icon">1️⃣</div>
        <h4>确认方案</h4>
        <p style="font-size:0.88rem;color:var(--muted)">
          团队评审本方案，确认架构、模块划分、技术栈选型
        </p>
      </div>
      <div class="card">
        <div class="icon">2️⃣</div>
        <h4>细化数据库</h4>
        <p style="font-size:0.88rem;color:var(--muted)">
          输出完整的建表 SQL 和 ER 图，评审后冻结 v1 schema
        </p>
      </div>
      <div class="card">
        <div class="icon">3️⃣</div>
        <h4>搭建工程框架</h4>
        <p style="font-size:0.88rem;color:var(--muted)">
          创建 Qt 工程、目录结构、基础库，三条线可以开始写代码
        </p>
      </div>
      <div class="card">
        <div class="icon">4️⃣</div>
        <h4>接口契约评审</h4>
        <p style="font-size:0.88rem;color:var(--muted)">
          各模块 Service 接口签名评审，达成一致后锁定
        </p>
      </div>
    </div>

    <div class="callout success">
      <strong>立即可以启动的工作：</strong>
      数据库 schema 设计和 Qt 工程框架搭建不需要等待其他工作，可以今天就开始。
      这两项完成后，三条开发线即可全面并行推进。
    </div>
  </section>

  <footer>
    <p style="text-align:center;color:var(--muted);font-size:0.8rem">
      Qt 自动化测试配置软件 · 开发设计方案 v1.0 · 2026-09-05
    </p>
  </footer>

</div>

<script src="./_shared/js/echarts.min.js"></script>
<script src="./_shared/js/mermaid.min.js"></script>
<script src="assets/charts.js"></script>
</body>
</html>
